# PROJEKT-HANDOFF — Kärra Matcher

> **Detta är den första filen som läses i varje ny session** (av människa och agent).
> Den ska alltid spegla **faktisk** status. Uppdateras enligt §0 p.14 i [`CLAUDE.md`](../CLAUDE.md)
> — i samma commit som ett issue stängs.
> Skriv ingen kod förrän ett issue är valt ur `Ready` och godkänt av människa.

---

## 🔎 Snabbstatus
- **Fas:** **M0 (15/15), M1 (17/17), M1.5 (4/4), M2 (6/6) och M3 klara** — `#40` truppvyn utgick 2026-09-10, se *Viktiga beslut*. Repot är publikt
- **UX-polish påbörjad (post-driftsättningstest).** Efter att v2 driftsatts och testats live började en runda gränssnittsputs. **`#251` (superadmin-konsolen) klar** — de fyra staplade sektionerna (Sporter/Klubbar/Trupper/Lag & admins) ligger nu bakom **flikar** (en i taget, tillgänglig tablist/tabpanel + piltangenter), och skapa-formulären är gömda bakom en **"Lägg till"-knapp** i stället för alltid öppna. Ny `Tabs`-komponent. Rent FE, 599 tester gröna. **Öppet härnäst:** navigationsflikarna (`AppNav`) byter innehåll beroende på sida (t.ex. "Tränare" vs "Chatt") — rapporterat, ska ses över.
- **SW offline-cache omprövad (`#249`)** — i stängda v2 märker backend varje API-svar `private, no-store` (#191), så service workern cachade i praktiken inget schema längre, trots att §KM.8 lovade offline-läsning. **Beslut (godkänt): nätet krävs för innehåll.** SW:n är nu **network-only för `/api/`** (privat medlemsdata ska inte ligga kvar på enheten); appskal + statiska tillgångar cachas fortfarande så den installerade appen öppnas offline och ger ett tydligt besked. Data-cachen borttagen (SW-version → `v3`). §KM.8 uppdaterad till v2-verkligheten. Rent FE, 598 tester gröna. **Därmed är hela FE-städningen klar** — inga fler poster på den listan.
- **Döda ICS-länkar borttagna (`#247`)** — den publika kalenderfeeden retirerades i `#191` (§KM.4), men FE:ns länkar (`CalendarLink`, `TeamCalendarLink` → `/calendar/*.ics`) fanns kvar och 404:ade. Borttagna med komponenter, exporter, service workerns `/calendar/`-gren och den oanvända CSS:en. Rättade även texter som lovade kalenderprenumeration (tränarvy, konto, integritet, enhets-push-notis) — och passade på att rätta två v1-texter som ännu påstod att innehåll fungerar "utan konto" (falskt i den stängda v2). Rent FE, 596 tester gröna. **FE-städningen har nu bara SW-offline-cache-omprövningen kvar (§KM.8).**
- **Klient-push (`#244`) klar** — push fungerar nu **end-to-end**. Klienten kan slå på notiser per lag (tillstånd → `pushManager.subscribe` mot VAPID-nyckeln från `GET /push/key` → `POST /teams/{slug}/push`) och av (`DELETE`), och service workern har `push`- (visar `{title,body,url}`) och `notificationclick`-hanterare (återanvänder en öppen flik). Ny enhetsväxel "Notiser på den här enheten" i notisinställningarna med tydliga tillstånd (stöds ej / iOS-hemskärm / nekat / på-av). Prenumerationen är enhetens och hålls i en egen localStorage-flagga; adressen loggas aldrig och når aldrig ett svar (§KM.10). **Buggfix:** samåkningsnotisernas djuplänk pekade på den utgångna `/match/`-adressen — nu `/handelse/{id}` (§KM.5/#198). BE 640 + FE 602 tester gröna. **Klient-push var en av de kvarvarande driftsättningsblockerarna — kvar nu bara migrering mot Neon.**
- **FE gäst-grind (`#242`) klar** — frontenden är nu stängd i linje med backend (§KM.3): en gäst på en innehållsroute (`/`, `/lag/$slug`, `/handelse/$id` + de tidigare skyddade) omdirigeras till `/logga-in?next=` i stället för att möta ett `401`. En delad `requireSession`-vakt ersätter 7 kopior av samma `beforeLoad`-block; de nu döda gäst-CTA:erna (`SignInLink` + carpool-gäst-varianterna) är borttagna, login-sidans text uppdaterad. Rent FE, ingen migration. 585 tester gröna. **En av två driftsättningsblockerare avklarad** (kvar: klient-push, migrering mot Neon; plus mindre FE-städning av döda ICS-länkar och SW-cache).
- **v2 (epic `#205`):** `#189`–`#204` klara. **`#206` (sportquiz i spelarkortet) klar** — en liten sportquiz på barnets spelarkortssida (`/spelarkort/$childId`), en kul grej mellan förälder och barn. **Allt device-only (§KM.2):** statisk frågebank som följer med appen, en fråga i taget med rätt/fel-besked i text, resultat med "spela igen". Rekordet sparas i en **egen** localStorage-nyckel (`karra.sportquiz`), skild från spelarkortets känsliga blob (ingen schema-migrering, ingen backup-koppling). Tester vaktar att quizet aldrig importerar API-lagret eller rör nätet. **Rent frontend — ingen backend, ingen migration.** FE 584 tester gröna. Med detta är **alla epic-`#205`-issues (#189–#206) byggda**; kvar innan v2 kan driftsättas är de stående uppgifterna nedan (FE gäst-grind, klient-push, migrering mot Neon).
- **v2 (tidigare):** `#189`–`#203` klara. **`#204` (audit-logg för roll-/medlemskapsändringar) klar** — själva audit-loggningen fanns redan (varje åtgärd i `#192`–`#197` skriver en rad *och* har ett test som verifierar den; sport/klubb/trupp/lag har ingen radering att logga — den funktionen finns inte, YAGNI). Det som saknades mot §KM.10 var **correlation-id på raden**: ny `AuditEntry.CorrelationId` fylls från requestens id (via `ICorrelationContext` i Application, `HttpCorrelationContext` i Api som läser `HttpContext.TraceIdentifier`), så id:t klienten får i `X-Correlation-Id` är samma id som står på audit-raden och i loggarna — ett ärende går att följa end-to-end. Migration `AddAuditCorrelationId` (en nullbar kolumn). Backend 636 tester gröna (+2 end-to-end correlation). **Migrationerna `#198`–`#201` + `AddAuditCorrelationId` fortfarande inte körda mot Neon.**
- **v2 (tidigare):** `#189`–`#202` klara. **`#203` (gallring/radering) klar** — kallelser med sina per-barn-svar gallras **30 dagar** efter händelsen (eget arbetar-jobb i processen, samma mönster som samåkning/chatt: dygnsintervall, idempotent, loggar bara antal, läser aldrig fritexten). Raderingen är **hård och kaskaderande**: två arkitekturtester räknar upp varje främmande nyckel mot ett **konto** resp. ett **barn** och fäller bygget om någon lägger till en tabell som inte försvinner med det principala — audit-noter (t.ex. `AttendanceCall.OpenedByAccountId`) saknar med flit FK och överlever som id utan namn. §KM.6 uppdaterad med gallringsfönstren (samåkning 30 d, kallelse 30 d, chatt 90 d) och kaskad-kravet. Backend 634 tester gröna. **Migrationerna `#198`–`#201` fortfarande inte körda mot Neon (#203 kräver ingen ny migration).**
- **v2 (tidigare):** **`#202` (chatt per lag) klar** — egna chatt-kanaler per färg-lag ovanpå trupp-chatten, byggda på samma modell (`ChatMessage.TeamId`). Bara lagets medlemmar (`MemberOfTeam`); kanalerna är isolerade från trupp-chatten; notis går till lagets medlemmar. **Moderering och gallring delas med trupp-chatten:** truppens admin-kö spänner alla kanaler och en kanalobunden admin-radering (`DELETE /api/v1/admin/trupper/{truppId}/chat/messages/{id}`) tar bort ett anmält meddelande oavsett kanal. FE: route `/lag/$slug/chatt` + "Lagchatt"-länk. Nästa: `#203` (gallring/radering). **Migrationerna `#198`–`#201` är inte körda mot Neon än (#202 kräver ingen ny migration).** Klient-push fortfarande obyggd (egen uppgift).
- **v2 (tidigare):** **`#201` (trupp-chatt) klar** — medlemschatt per trupp, notis (respekterar Chatt-val), moderering (radera/anmäl + admins kö), **90 d gallring**, **schemalagda meddelanden** för admin/tränare. Se *Viktiga beslut*.
- **v2 (tidigare):** **`#200` (aviseringar per medlemskap) klar** — en lag-notis når bara lagets **medlemmar** (aldrig anonyma), per-typ **Händelser / Kallelser / Samåkning / Chatt**. Riktade utskick oförändrade. Se *Viktiga beslut*.
- **v2 (tidigare):** **`#199` (riktad kallelse per barn) klar** — antals-närvaron ersatt av kallelse **per barn**: admin kallar utvalda barn **tvärs över lagen**, vårdnadshavare svarar **Ja/Nej** per barn, sammanställning + påminnelse. Bakom `AttendanceEnabled`-grinden. Se *Viktiga beslut*.
- **v2 (tidigare):** **`#198` (event-modell) klar** — match-only omgjord till generisk **händelse** (match/träning/övrigt), full Match→Event-omdöpning BE+FE, route `/handelse/$id`. **Medveten gräns:** närvaro och samåkning behåller sitt interna `MatchId` (pekar på Events). Se *Viktiga beslut*.
- **Senast uppdaterad:** 2026-09-24 (#251) av Haval
- **Aktuell milstolpe:** M7 — Notiser (2 av 6 klara). M5 klar 6 av 6. M6 omskriven 2026-09-10 utan barnuppgifter: `#56` klar, `#57`/`#58` kvar
- **Hälsa:** 🟢 på plan — appen är i drift och användbar för föräldrar utan konto

## 🧱 Teknikstack (bekräftad)
- **Backend:** C# / .NET (senaste LTS), EF Core, MediatR, FluentValidation, PostgreSQL via Npgsql
- **Frontend:** React + TypeScript (Vite), TanStack Router, TanStack Query, React Hook Form + Zod
- **Klient:** PWA — installerbar, offline-läsbart schema. Ingen app store
- **Repo:** Monorepo — `backend/` och `frontend/` i samma repo
- **Notiser:** Web Push (VAPID) + ICS-kalenderfeed per lag
- **Drift:** Frontend på **Vercel**, backend som Docker-container på **Render**, databas på **Neon**.
  `frontend/vercel.json` rewriter `/api/*` till Render → klienten ser en enda origin. Allt på fria nivåer.
  Samma uppsättning som `source/repos/CarCheck` (carcheck.se), som redan är i drift.

## 🔗 Viktiga länkar
| Resurs | Länk |
|--------|------|
| Projektboard (GitHub Projects) | https://github.com/users/Haval-Jalal/projects/7 |
| Repo | https://github.com/Haval-Jalal/KarraMatcher *(publikt)* |
| Regelverk (HUR) | [`CLAUDE.md`](../CLAUDE.md) |
| Produktspec (VAD & VARFÖR) | [`SPEC.md`](../SPEC.md) |
| Standarder vid behov | [`STANDARDER-VID-BEHOV.md`](../STANDARDER-VID-BEHOV.md) |
| Säkerhetschecklista (releasegrind) | [`SAKERHET-CHECKLISTA.md`](../SAKERHET-CHECKLISTA.md) |
| Plan / roadmap | [`MVP-PLAN.md`](./MVP-PLAN.md) |
| Backup och återställning | [`DATABAS-BACKUP.md`](./DATABAS-BACKUP.md) — läses när något gått fel |
| Driftövervakning | [`DRIFTOVERVAKNING.md`](./DRIFTOVERVAKNING.md) — pingschema och timbudget |
| Ursprungsmallar | Förvaras utanför repot |
| Förstudieplan (artefakt) | https://claude.ai/code/artifact/9fc04494-e56d-4510-acf7-28231d64955a |
| Backend i drift (Render) | Live sedan 2026-08-29. URL:en finns på exakt ett ställe: [`frontend/vercel.json`](../frontend/vercel.json) (§KM.11) |
| Frontend i drift (Vercel) | https://karra-matcher.vercel.app — live sedan 2026-08-29 |

## ✅ Klart hittills

**M1 verifierad på riktiga enheter 2026-08-30.** Tre prov som inte gick att göra härifrån:

| Prov | Resultat |
|---|---|
| Kalenderprenumeration | `P2016 Gul`, 6 matcher, dagens match **13:15** — i både iOS Kalender och Google Kalender. Rätt tid bevisar att UTC med `Z` var rätt val framför en egen `VTIMEZONE` |
| Installation på hemskärmen | Fungerar på iPhone och Android. Rätt ikon, inget adressfält |
| Offline | Schemat står kvar i flygplansläge på båda, även efter att appen stängts helt |
| Skärmläsare | VoiceOver och TalkBack: hopplänken först, lagen som "länk", aktuellt lag som "aktuell sida", matchkort läses som tid, datum, motståndare och plats |

- `#61` Bakgrundsjobb för notisutskick, med VAPID-signering och RFC 8291-kryptering — 2026-09-10
- `#60` VAPID-nycklar och anonym prenumeration per lag — 2026-09-10
- `#56` Feature flag `AttendanceEnabled` med serverside-grind och adminspak — 2026-09-10
- `#154` Namn på kontot — samåkningen fick ett ansikte. Rättade samtidigt att `getJson` aldrig skickade access-token, så inloggade GET-anrop lästes som gäst — 2026-09-10
- `#55` Tränarens samåkningsöverblick och gallring efter 30 dagar — **M5 klar** — 2026-09-10
- `#54` Gästen leds till inloggning i stället för att mötas av en saknad knapp — 2026-09-10
- `#53` Samåkningsvyn: erbjuda, fråga, svara — hela flödet på matchsidan — 2026-09-10
- `#150` Inloggningen sa att koden inte gick att skicka trots att mejlet var skickat — tomt 202-svar tolkades som fel — 2026-09-10
- `#148` Huvudmeny, så appens delar går att nå — 2026-09-10
- `#28` Tillgänglighetsgenomgång: två AA-fel rättade, kraven låsta i test — 2026-08-30
- `#26` PWA: manifest, ikoner, service worker och offline-läsbart schema — 2026-08-30
- `#110` Spelplatsernas koordinater rättade — 2026-08-30
- `#108` Vägbeskrivningen begär bilrutt — 2026-08-30
- `#22` Väder vid avspark via Open-Meteo — 2026-08-30
- `#24` Kalenderfil per match, och prenumerationslänk på lagsidan — 2026-08-30
- `#25` ICS-prenumeration per lag, med SEQUENCE och CANCELLED — 2026-08-30
- `#23` Vägbeskrivning till spelplatsen, rätt kartapp per enhet — 2026-08-30
- `#21` Matchdetaljsida på egen adress, nådd från listan och nästa match-kortet — 2026-08-30
- `#17` Publik endpoint för enskild match — 2026-08-30
- `#100` Nästa match visades två gånger — rättad — 2026-08-30
- `#20` Nästa match-kort med relativ dag — 2026-08-30
- `#19` Matchlista med månadsrubriker och hopfällbar historik, på egen adress per lag — 2026-08-30
- `#18` Lagväljare med lagfärgen som tema, API-klient och Vite-dev-proxy — 2026-08-30
- `#27` Tidszonshantering i frontenden, med ESLint-regel som håller konverteringen på ett ställe — 2026-08-30
- `#92` Edge-cachningen verifierad i skarp drift — §KM.11:s antagande bevisat — 2026-08-30
- `#16` Publika endpoints för lag och matcher, med egen query-dispatcher — 2026-08-30
- `#15` Uppetidsövervakning med verifierat larm — **M0 därmed klar** — 2026-08-30
- `#14` Databasbackup med bevisad återställning; PITR 6 h bekräftad — 2026-08-29
- `#13` Edge-cache-mekanism med ETag och säker standard — 2026-08-29
- `#10` Arkitekturtester: lagergränser, entiteter i controllers, §KM.2-skyddet — 2026-08-29
- `#12` `vercel.json` med `/api`-rewrite; frontend i drift på Vercel — 2026-08-29
- `#11` Dockerfile och backend i drift på Render — 2026-08-29
- `#9` Rate limiting på publika endpoints — 2026-08-29
- `#8` Serilog, correlation-ID, ProblemDetails och health checks — 2026-08-29
- `#7` Idempotent seed av 4 lag, 7 spelplatser och 25 matcher — 2026-08-29
- `#6` Neon Postgres och EF Core med första migrationen — 2026-08-29
- `#5` CI som kör bygge, tester och lint på varje push — 2026-08-29
- `#4` Lint, formatering och `.env.example` — 2026-08-29
- `#3` Vite-frontend med strict TypeScript, TanStack Router och Query — 2026-08-29
- `#2` .NET-lösningen med fyra lager, fyra testprojekt och arkitekturtester — 2026-08-29
- `#1` .NET 10 LTS installerat och låst med `global.json` — 2026-08-29
- Repo, projektboard och 73 issues fördelade på nio milstolpar — 2026-08-29
- Genomgång av båda projektmallarna i sin helhet — 2026-08-29
- Produktbeslut fattade (roller, statistikmodell, kallelse avstängd, omfattning) — 2026-08-29
- Regelverk, spec, standarder och säkerhetschecklista skrivna och anpassade — 2026-08-29

## 🚧 Pågår nu
| Issue | Vem | Branch | Status |
|-------|-----|--------|--------|
| `#244` Klient-push | Haval | `feature/v2-client-push` | In Review — enhetsprenumeration (tillstånd → `pushManager.subscribe` → `POST /teams/{slug}/push`, av via `DELETE`), service worker `push`/`notificationclick`, enhetsväxel i notisinställningarna; buggfix `/match/`→`/handelse/` i samåkningsnotiser; BE 640 + FE 602 tester gröna |

## ➡️ Nästa steg

**v2 (epic `#205`) — alla issues byggda.** `#189`–`#206` klara. FE gäst-grind (`#242`) och
klient-push (`#244`) därutöver klara. **Kvar innan v2 kan driftsättas: bara migreringen mot Neon**
— v2-migrationerna (`AddV2DomainModel` → `AddAuditCorrelationId`) är incheckade men inte körda
mot databasen än. **Runbook finns:** [`docs/DEPLOY-NEON-MIGRATION.md`](./DEPLOY-NEON-MIGRATION.md)
— säkerhetskopiera först (den enda oåterkalleliga raden är en `DROP TABLE "AttendanceResponses"`,
den gamla antalsnärvaron; `Matches`→`Events` byter bara namn och behåller data), granska den
idempotenta SQL:en, applicera via skript eller `Database__ApplyMigrationsOnStartup=true`, verifiera.
Mindre FE-städning kvar som egna uppgifter (se nedan).

**Två saker att bära med efter `#191`:**

1. **Frontend-grinden finns nu (`#242`).** Gäst-grinden är byggd: en gäst på en innehållsroute
   omdirigeras till `/logga-in?next=` i stället för att möta ett `401`, och alla content-routes
   (`/`, `/lag/$slug`, `/handelse/$id` + de tidigare skyddade) går genom en delad `requireSession`.
   **FE-städningen är klar:** de döda ICS-/kalenderlänkarna borttagna (`#247`) och service
   workerns offline-cache omprövad (`#249`, network-only för `/api/`, §KM.8 uppdaterad). Inga
   fler poster på den listan.
2. **Driftsätt backend och FE samordnat.** Merge till `main` bryter inget, men den stängda
   `#191`-backend och FE-grinden hör ihop — driftsätt dem tillsammans, inte var för sig.

## 🧭 Viktiga beslut (ADR-light)

| Datum | Beslut | Motivering | Konsekvens |
|-------|--------|------------|-----------|
| 2026-09-23 | **v2: SW cachar inte längre autentiserat innehåll — nätet krävs för schemat** (`#249`) | §KM.8 lovade offline-läsning av schemat, men i stängda v2 (#191) märker backend varje API-svar `private, no-store`, så SW:ns `mayStore` refuserade redan att cacha det — offline-schemat var trasigt i praktiken. Att i stället medvetet cacha medlemmens privata data på enheten vore att gå emot #191:s `no-store` och lägga privat lagdata där den inte behövs. **Beslut (godkänt av användaren): nätet krävs för innehåll** framför att återinföra en privat-data-cache. | Service workern är nu **network-only för `/api/`** (låter anropet passera; ingen cache-skrivning, ingen cache-läsning). Appskalet och statiska tillgångar (ikoner, manifest) cachas fortfarande — den installerade appen öppnas offline och ger ett tydligt svenskt besked i stället för webbläsarens felsida; `no-store`/`private`-guarden är kvar för det som ändå cachas. Data-cachen (`karra-data-*`) borttagen och SW-versionen höjd till `v3` så gamla data-cachar städas bort. §KM.8 omskriven: offline-medveten för skalet, innehåll kräver uppkoppling. Push (#244) når ändå fram vid ändringar. Spelarkortet (device-only, §KM.2) berörs inte — det ligger i enhetens egen lagring, aldrig i sw-cachen. Rent FE, ingen migration. 598 tester gröna. **FE-städningen därmed helt klar.** |
| 2026-09-23 | **v2: bort med de döda ICS-/kalenderlänkarna** (`#247`) | Den publika kalenderfeeden retirerades i `#191` (§KM.4, stängd app), men FE:ns länkar och komponenter fanns kvar och pekade på `/calendar/*.ics` som nu 404:ar — en knapp som inte gör något, och texter som lovar en prenumeration som inte finns. | Borttaget: `CalendarLink` (`/calendar/handelse/{id}.ics`) och `TeamCalendarLink` (`webcal://…/calendar/{slug}.ics`) med komponenter, användningar (`EventDetailPage`, `EventListSection`) och exporter; service workerns `/calendar/`-undantag och den oanvända `.calendar-subscribe`-CSS:en; samt de tester som verifierade länkarna. Rättade texter som lovade feeden (tränarvyn "kalenderprenumerationerna uppdateras"/"kvar i kalendern", `DevicePushToggle` "Schemat och kalendern", schema.ts-kommentar om ICS-feeden). **Passade på** att rätta två v1-texter (`AccountPage`, `PrivacyPage`) som ännu påstod att matchtider m.m. fungerar "utan konto" — falskt sedan `#191`/`#242` stängde appen; nu ärliga om att lagets innehåll kräver inloggning och att bara spelarkortet är device-only. Rent FE, ingen migration. 596 tester gröna. Kvar av FE-städningen: bara SW-offline-cache-omprövningen (§KM.8). |
| 2026-09-23 | **v2: klient-push — enhetsprenumeration + service worker-notiser** (`#244`) | Backendens push-utskick fanns sedan `#60`–`#68`, men ingen enhet prenumererade eller visade något — notiserna nådde aldrig en telefon. Sista biten för end-to-end push. | Ny `lib/push.ts`: `enableTeamPush`/`disableTeamPush`, tillstånd, stödkoll och `urlBase64ToUint8Array`. Flödet: begär tillstånd → hämta VAPID-nyckel (`GET /push/key`) → `pushManager.subscribe({ userVisibleOnly: true })` → `POST /teams/{slug}/push`; av via `DELETE` (webbläsarens prenumeration lämnas kvar, den kan delas av ett annat lag). Service workern (`public/sw.js`, cache `v2`) fick `push`- (visar `{title,body,url}`, aldrig PII) och `notificationclick`-hanterare (återanvänder en öppen flik hellre än att öppna en till). Ny `DevicePushToggle` överst i `NotificationSettings` med ärliga tillstånd (stöds ej / iOS kräver hemskärm / nekat / på-av); kategoriväxlarna (`#65`) styr *vad*, den nya styr *om*. **Prenumerationen är enhetens** — den hålls i en egen `localStorage`-flagga per lag (servern har ingen läs-endpoint, och adressen får aldrig lämna enheten i ett svar, §KM.10). **Buggfix i samma PR:** `CarpoolNotification` byggde den utgångna djuplänken `/match/{id}`; nu `/handelse/{id}` (`#198`) med ett test som låser det. Ingen migration. BE 640 (+4) + FE 602 (+17) tester gröna. **Klient-prenumerationen var en av de kvarvarande driftsättningsblockerarna — kvar nu bara migreringen mot Neon.** |
| 2026-09-23 | **v2: FE gäst-grind — allt innehåll bakom inloggning** (`#242`) | Backend stängdes i `#191` (§KM.3), men FE saknade grinden: en gäst på `/`, `/lag/$slug` eller `/handelse/$id` gjorde ett anrop som backend svarade `401` på och möttes av en trasig/tom vy. En delad länk till schema/händelse ska nu leda till inloggning, inte in i en vägg. Beslut (godkänt av användaren): **full §KM.3-grind** — även de tidigare "publika" schema-/händelselänkarna kräver inloggning. | Ny delad `requireSession(pathname)` i routern: förnyar sessionen om det finns en hint, annars `throw redirect({ to: '/logga-in', search: { next } })`. Den ersätter **7 ordagranna kopior** av samma `beforeLoad`-block och läggs nu även på `indexRoute`, `teamRoute` och `eventRoute`. Publikt kvar (anonymt tillåtet, §KM.3): `/logga-in`, `/inbjudan/$token`, `/ansok/$truppId`, `/integritet` och spelarkortet (device-only, §KM.2). **Följd:** de inline gäst-CTA:erna (`SignInLink` + carpool-gäst-varianterna "Logga in för att samåka/fråga") blev död kod — en gäst når aldrig den vyn — och togs bort; `CarpoolOfferCard`/`CarpoolSection` slutar ta emot `isSignedIn`. Login-sidans text rättad (inte längre "matchtiderna är öppna för alla"). Tester som antog öppet läge vändes: gäst → inloggning för schema/händelse/start (nya `routing`-tester äger `next`-kontraktet), övriga loggar in först. Rent FE, ingen migration, inga PII-kolumner. 585 FE-tester gröna. **En av två driftsättningsblockerare avklarad.** |
| 2026-09-23 | **v2: sportquiz i spelarkortet, device-only** (`#206`) | Sista epic-issuet. En kul grej mellan förälder och barn i spelarkortsvyn, i samma anda som kortet självt (§KM.2). Placerad på barnets kortsida (`/spelarkort/$childId`), inte på server-plattformen — quizet hör ihop med det device-only kortet, inte med v2:s serverdel. | Rent frontend under `features/playercard/quiz/`: statisk frågebank (`questions.ts`, svenska, barnvänliga, 5 frågor/omgång slumpat), `SportQuiz.tsx` (vila → en fråga i taget med rätt/fel-besked i **text** och fokusflytt, inte färg ensam → resultat med "spela igen"), och ett rekord i en **egen** localStorage-nyckel `karra.sportquiz`. **Rekordet hålls medvetet utanför spelarkortets blob** — en trivial kul-siffra ska inte tvinga en migrering av det känsliga, backup-bara schemat (`CURRENT_VERSION` orörd). §KM.2 vaktat av tester: quizets filer importerar aldrig `@/lib/api` och rör inte nätet när man spelar. Ingen backend, ingen migration, inga nya PII-kolumner. FE 584 tester gröna (+17). **Alla epic-`#205`-issues därmed byggda (#189–#206).** |
| 2026-09-23 | **v2: correlation-id på audit-raden** (`#204`) | §KM.10 kräver audit-logg *och* correlation-id per request genom loggarna. Audit-loggningen fanns redan (byggd inkrementellt i `#192`–`#197`: varje känslig roll-/medlemskapsåtgärd skriver en rad och har ett test; sport/klubb/trupp/lag saknar radering att logga — funktionen finns inte, YAGNI). Det som saknades var att kunna **koppla en audit-rad till requestens loggrader**. | Ny `AuditEntry.CorrelationId` (nullbar, max 64) fylls från requestens id via ny abstraktion `ICorrelationContext` (Application) med implementationen `HttpCorrelationContext` (Api, läser `HttpContext.TraceIdentifier` — samma värde `CorrelationIdMiddleware` sätter och klienten får i `X-Correlation-Id`). AuditLog (Infrastructure) injicerar abstraktionen; **Infrastructure förblir HTTP-fritt** och arkitekturtesterna passerar. Två end-to-end-tester: id klienten skickar in respektive serverns genererade id hamnar på audit-raden. Migration `AddAuditCorrelationId` (en kolumn), inga nya PII-kolumner (ett request-id är ingen personuppgift, §KM.10). Backend 636 gröna. **Migrationerna `#198`–`#201` + denna ej körda mot Neon.** |
| 2026-09-23 | **v2: gallring & hård kaskad-radering** (`#203`) | En stängd app med barn-profiler och fritext (§KM.1/§KM.6) måste både radera direkt och glömma med tiden. Radering skulle inte vila på att någon minns att stänga varje tabell, och gallringen inte på cron (Vercel Hobby ger ett dygns-cron, lovat till kvällspåminnelsen). | **Kallelse-gallring 30 dagar** efter händelsen (`AttendanceRetentionService`/`-Repository`/`-Worker`) — svaren (`AttendanceInvitation`) tas med kallelsen (`AttendanceCall`), jobbet kör i processen (dygnsintervall, 50 s startfördröjning, idempotent, `[LoggerMessage]` loggar bara antal, läser aldrig fritext, ett fel fäller aldrig API:t). Speglar samåkningens/chattens jobb. **Hård kaskad vaktad i schemat:** utöver den befintliga `AlltSomPekarPaEttKonto_ForsvinnerMedDet` finns nu `AlltSomPekarPaEttBarn_ForsvinnerMedDet` — båda räknar upp varje FK mot principalen och kräver `Cascade`, så en ny tabell som pekar på ett konto eller ett barn utan att försvinna med det **fäller bygget**. Namngivna undantag: audit-noter utan FK (`AttendanceCall.OpenedByAccountId`, `AttendanceInvitation.RespondedByAccountId`, `ChatMessage.DeletedByAccountId`) överlever som id utan namn/e-post (§KM.10); push-prenumerationens kontokoppling är `SetNull` (#63). §KM.6 uppdaterad med gallringsfönstren (samåkning 30 d, kallelse 30 d, chatt 90 d) och kaskad-kravet. Ingen ny migration, inga nya PII-kolumner. Backend 634 tester gröna (+3 gallring, +1 barn-kaskad-vakt). **Migrationerna `#198`–`#201` fortfarande ej körda mot Neon.** |
| 2026-09-21 | **v2: chatt-kanaler per lag ovanpå trupp-chatten** (`#202`) | Färg-lagen behöver egna kanaler (matchlogistik) ovanpå trupp-chatten. Byggt på #201:s modell — inget nytt datalager, ingen ny migration. Chatten är nu **kanal-medveten**: `ChatMessage.TeamId` null = trupp-kanal, satt = lag-kanal; tjänsten tar `(truppId, teamId?)` och trupp-endpointen skickar null (bakåtkompatibelt). Nya lag-endpoints under `/api/v1/teams/{slug}/chat` (`MemberOfTeam`, slug → (teamId, ageGroupId)); kanalerna är isolerade; notis går till **lagets** medlemmar (`MemberAccountIdsAsync(teamId)`). **Delad moderering/gallring (Klart när):** truppens admin-kö `GET /admin/trupper/{truppId}/chat/reports` spänner nu trupp- OCH alla lag-kanaler, och en **kanalobunden admin-radering** `DELETE /admin/trupper/{truppId}/chat/messages/{id}` (`AdminOfTrupp`) tar bort ett anmält meddelande oavsett kanal — infört efter att FE-forken korrekt flaggade att medlems-radera-endpointen 404:ar på ett lag-meddelande från trupp-kön. Gallringen (90 d) täcker redan alla kanaler. Ny grind ingen (återanvänder `MemberOfTeam`); FE `/lag/$slug/chatt` + "Lagchatt"-länk och en kanal-medveten `ChatChannel`-komponent. Tester + FE via två fork-agenter. 630 backend- + 564 FE-tester gröna. |
| 2026-09-21 | **v2: trupp-chatt med moderering, gallring och schemaläggning** (`#201`) | En stängd trupp behöver en gemensam kanal. Fritext är potentiell PII (§KM.1/§KM.10). **§KM.1-BESLUT: ny fritextkolumn `ChatMessage.Body`** (och `ChatReport`) införs medvetet — den kan innehålla vad en förälder skriver, men vi skriver aldrig själva ett barns namn där; den loggas aldrig (§KM.10), töms direkt vid radering, och gallras. **Beslut (godkänt av användaren): svar/format** — medlemmar läser/skriver; **gallring 90 dagar**; **rapportera → truppens admin** ser anmälningskön och kan radera; **radera** = eget alltid, admins vad som helst (texten töms, "[borttaget]" kvar); **schemalagda meddelanden** som ett extra val för **admin/tränare** (inte en fråga varje gång). Ny `ChatMessage` (trupp `AgeGroupId` + nullbart `TeamId` — null = trupp-kanal, `#202` sätter lag) och `ChatReport`. Ny grind **`MemberOfTrupp`** + membership-metoder (`IsMemberOfTrupp`/`IsLeaderOfTrupp`/`MemberAccountIdsForTrupp`/`MemberTrupper`). Notis: `PushCategory.Chat` till truppens medlemmar utom författaren, och "Chatt av någonstans i truppen = av" (per-lag-inställningen tolkad konservativt för en trupp-bred chatt); aldrig text/barnnamn i pushen. Släpp av schemalagt via `ChatReleaseWorker` (~1 min; Render-sömn kan fördröja några min — dokumenterat). Gallring via `ChatRetentionService`/`Worker` (i processen, inget nytt cron). Endpoints: medlem under `/api/v1/trupper/{truppId}/chat`, admin-kö under `/api/v1/admin/trupper/{truppId}/chat/reports`, och `/api/v1/trupper/mina` så en medlem hittar sin chatt. **Klient-prenumerationen på push är fortfarande obyggd (egen uppgift)** — notisen skickas server-side men når ingen enhet förrän klient-flödet byggs. Kärnan byggd för hand; tester + FE via två fork-agenter mot fast kontrakt. 620 backend- + 559 FE-tester gröna. **Migrationen `AddChat` ej körd mot Neon än.** |
| 2026-09-21 | **v2: aviseringar per medlemskap + målgrupp, per-typ-inställningar** (`#200`) | Den öppna appens push gick till *alla* som prenumererade på ett lag (även anonyma). Stängd app (§KM.3): en notis ska bara nå **medlemmar** i rätt målgrupp. **Beslut (godkänt): (A) ta med chatt-inställningen redan nu** fast utskicket byggs i #201/#202; **(egen issue) klient-prenumerationen** (tillstånds-UX + service worker) byggs separat — #200 = server-sidans scoping + inställningar (vilket är issuens "Klart när"). Lag-utskick (`ToTeam`) filtreras nu mot lagets medlemmar via ny `IMembershipService.MemberAccountIdsAsync` (tränare + trupp-admins + vårdnadshavare till barn i laget + accepterade inbjudningar/godkända ansökningar; **global superadmin utelämnas** — ska inte spammas per lag); anonyma prenumerationer (utan konto) nås aldrig. Riktade utskick (`ToAccounts`) är **oförändrade** — det är så en kallelse når ett **inlånat barns** vårdnadshavare (som inte är medlem i händelsens lag) och samåkning når rätt personer; membership-grinden får inte läggas där. `PushCategory` → `EventChange/Kallelse/Carpool/Chat`; `NotificationPreference` → `EventChanges/Kallelser/Carpool/Chat` (migration döper om kolumnerna, lägger till `Chat` default på). Händelseändring + kvällspåminnelse = `EventChange`; ny kallelse + påminnelse = `Kallelse`. FE-inställningarna + GDPR-exporten uppdaterade till de fyra typerna. **Beteende-täckning** (medlem nås, icke-medlem/anonym inte; per-typ av tystar) ersätter den statiska grind-vakten för kallelse-endpoints. Backend 599 + FE 554 gröna. **Migrationen ej körd mot Neon än.** |
| 2026-09-21 | **v2: riktad kallelse per barn ersätter antals-närvaron (§KM.7)** (`#199`) | Den öppna appens närvaro var en antals-modell per konto (förälder svarade med en siffra 0–4), avstängd i drift. §KM.7 v2 kräver svar **per barn**, och truppen är organiserad så att ett lag fylls på med barn ur andra lag vid match. **Beslut (godkänt, bekräftat av användaren): svar Ja/Nej per barn (inget "kanske"); ersätt den gamla modellen; kallelsen riktas mot utvalda barn ur hela truppen (snabbval hela laget/hela truppen + enskilda tvärs över lag); behörighet = admin för truppen** (alla P16-tränare är admins), inte lag-tränare. Ny `AttendanceInvitation` (per kallat barn, nullbart `AttendanceReply {Coming,NotComing}`, FK-löst `RespondedByAccountId` som audit) ersätter `AttendanceResponse`; `AttendanceCall` (en per händelse) behålls; migration släpper den tomma antals-tabellen och skapar inbjudnings-tabellen (närvaron var av → ingen data tappad). Endpoints: vårdnadshavare `GET/PUT /api/v1/events/{eventId}/kallelse[/children/{childId}]` (`[Authorize]`, objektnivå per barn via `Guardianship` — **inte** `MemberOfEvent`, eftersom ett inlånat barns vårdnadshavare inte är medlem i händelsens lag); admin `PUT/GET/remind` under `/api/v1/admin/trupper/{truppId}/events/{eventId}/kallelse` (`AdminOfTrupp`). Kvar bakom `AttendanceEnabled`-grinden (grind av → vårdnadshavarens GET 404, adminens PUT 409); grinden kollas i tjänsten (kallelse-endpoints bär inte `[RequireAttendanceEnabled]`, så den statiska grind-vakten täcker dem inte — invarianten testas i stället beteendemässigt). Notis till de kallade barnens vårdnadshavare, aldrig barnets namn (§KM.1). `EventDetail` fick `truppId` så adminvyn kan hämta rostern (#196). GDPR-exporten ändrad till svar-per-barn. Barn visas som "Liam J". Kärnrenamet + tester via två fork-agenter mot fast kontrakt. 596 backend- + 554 FE-tester gröna. **Migrationen ej körd mot Neon än.** |
| 2026-09-18 | **v2: generisk Event-modell (match/träning/övrigt) ersätter match-only** (`#198`) | En stängd truppplattform behöver mer än matcher — träningar och övriga händelser — och riktade kallelser (`#199`) ska hänga på en generell händelse, inte på "match". **Beslut (godkänt): full omdöpning inkl. endpoints + FE-routes (alt. 1B); målgrupp skjuts till `#199` (alt. 2A).** Domänen `Match`→`Event` med `EventType {Match,Training,Other}` + `Title`; `OpponentName`/`IsHome` är nullbara (bara för match). Tabellen `Matches`→`Events` via en **handskriven `RenameTable`-migration** som backfillar `Type='Match'` — EF:s egen scaffold gjorde drop/create och hade raderat matchschemat, så migrationen skrevs om för hand. Endpoints `/api/v1/events` + `/api/v1/teams/{slug}/events`; policyn `MemberOfMatch`→`MemberOfEvent`; audit `handelse.*`; påminnelsejobbet generaliserat. **FE:** `features/matches`→`features/events`, route `/match/$id`→`/handelse/$id`, typ `TeamEvent`/`EventDetail`/`TeamEventSchedule` (undviker DOM-globala `Event`), och tränarens `EventForm` har en typväljare (match → motståndare + hemma/borta; träning/övrigt → rubrik). **Medveten gräns (revidera vid behov):** närvaro (`AttendanceCall`/`Response`) och samåkning (`CarpoolOffer`) behåller sin `MatchId`-kolumn/param — den pekar nu på `Events` och de är kvar scopeade till match-typade händelser; att döpa om hela deras yta låg utanför slicen. Träning/övrigt får ännu inte närvaro/samåkning i UI:t. Kärnrenamet skedde via två fork-agenter (BE-tester, FE) mot delade kontraktet. 602 backend- + 560 FE-tester gröna; migrationen ej körd mot Neon än (körs vid driftsättning). |
| 2026-09-18 | **v2: tränare tillsätts per lag av trupp-admin, objektnivå-auktorisering** (`#197`) | En stängd plattform behöver att admin kan tillsätta tränare per färg-lag; tränaren ska nå bara sitt lag. Domänmodellen och rollerna fanns redan (`#190`/`#192`) | **Ingen ny tabell, ingen ny PII-kolumn** — rollen är en `TeamRole{Coach, TeamId}` (finns sedan `#190`), och tränaranspråket (`coach:<lagets slug>`) byggs redan av `RoleRepository`, så tillsättningen låser automatiskt upp lagets endpoints via `CoachOfTeam`. Ny `CoachRoleService` (speglar `AdminRoleService`): tillsätt/avsätt en tränare på ett **befintligt konto** via adress (saknas kontot → 400 "bjud in först" `#193`; dubblett → 409). **Objektnivå:** både tillsättning och avsättning kontrollerar att laget hör till truppen i adressen — en admin för trupp A kan inte röra ett lag i trupp B genom att gissa lag-id (404, IDOR-skydd). Endpoints under `/api/v1/admin/trupper/{truppId}` (`[AdminOfTrupp]` + CSRF): `GET coaches` (lag + deras tränare), `POST teams/{teamId}/coaches` (adress), `DELETE teams/{teamId}/coaches/{accountId}`. Nya `AuditActions` `tranare.tillsatt`/`tranare.avsatt`, id-only (§KM.10). **FE:** återanvändbar `CoachesPanel` (per lag: lista tränare, tillsätt via adress, ta bort) i trupp-adminvyn (`/admin`) och superadmin-konsolen. 6 backend- + 3 FE-tester; backend 602 + FE 558 gröna |
| 2026-09-18 | **v2: barn på servern + sortering i färg-lag (§KM.1), koppling samtyckesgrindad (§KM.6)** (`#196`) | En stängd plattform behöver att admin kan lägga in truppens barn, sortera dem i färg-lag och koppla vårdnadshavare — grunden för riktade kallelser (`#199`). Rör barn under 13, så minsta möjliga data och samtycke innan koppling | **Ingen ny PII-kolumn och ingen ny tabell** — `Child`/`Guardianship` finns sedan `#190`; §KM.1 hålls (förnamn + efternamnets initial, visas "Liam J", aldrig hela efternamnet). **Beslut (godkänt): roster först, koppling samtyckesgrindad.** `IChildRepository`/`ChildRepository` + `ChildAdminService` (skapa/ändra/ta bort barn, flytta mellan lag; hård radering kaskaderar vårdnadshavarskap). **Koppling av vårdnadshavare kräver: kontot finns (annars 400 "bjud in först") → är trupp-medlem (accepterad inbjudan eller godkänd ansökan, annars 400) → har aktuellt samtycke via `HasCurrentConsentAsync` (annars 409 "Samtycke saknas §KM.6") → inte redan kopplad (409).** Endpoints under `/api/v1/admin/trupper/{truppId}/children` (`[AdminOfTrupp]` + CSRF, objektnivå: varje barn kontrolleras höra till truppen → IDOR ger 404): `GET` roster (lag + barn + vårdnadshavare), `POST`/`PUT`/`DELETE` barn, `POST`/`DELETE .../{id}/guardians`. Nya `AuditActions` (barn skapat/ändrat/raderat, vårdnadshavare kopplad/bortkopplad), id-only (§KM.10). **FE:** återanvändbar `ChildrenPanel` (roster grupperad per färg-lag + "Otilldelade", skapa/ändra/flytta/ta bort, koppla/koppla-bort med tydligt samtyckesfel) i både trupp-adminvyn (`/admin`) och superadmin-konsolen. 10 backend- + 3 FE-tester; backend 596 + FE 555 gröna |
| 2026-09-16 | **v2: vårdnadshavarsamtycke (§KM.6) — versionerad text, samtycke per vårdnadshavare** (`#195`) | Barn lagras nu på servern (§KM.1), så samtycke krävs innan ett barn kopplas. Behöver kunna svara på *vad* någon samtyckte till och *när* (GDPR) | **Beslut (godkänt): samtycke per vårdnadshavare** (inte per barn) — en vuxen samtycker en gång till den aktuella versionen, vilket låser upp koppling av deras barn i `#196`. **Textlagring: versionsregister i kod** (`ConsentDocument`, version→text) så exakt den godkända texten alltid kan visas även efter uppdatering; gamla versioner tas aldrig bort. Ny **`GuardianConsent`**-entitet (konto + version + tid, unikt per konto+version, cascade vid kontoradering) + migration. `ConsentService`: ge samtycke (bara aktuell version, idempotent; stale version → `409`), "mitt samtycke" (vad/när + texten), och `HasCurrentConsentAsync` som **`#196`** använder för att kräva samtycke innan koppling. Endpoints under `/api/v1/consent` (`[Authorize]`): `GET current`, `GET me`, `POST` (samtyck). Ny `AuditAction` `samtycke.givet`, id-only. **FE:** `ConsentSection` på Mitt konto — läs texten + "Jag samtycker", och därefter "Du har samtyckt till version X den …" med utfällbar text. 5 backend- + 2 FE-tester; backend 586 + FE 552 gröna |
| 2026-09-16 | **v2: ansökningar — förälder ansöker, admin godkänner; godkänd ansökan = trupp-medlemskap** (`#194`) | Den andra vägen in vid sidan av inbjudan (`#193`): föräldern tar initiativet via en delad ansökningslänk och en admin godkänner. Delar medlemskapsmodellen med inbjudningarna | Ny **`MembershipApplication`**-entitet + migration `AddApplications`. `MembershipService` läser nu **fyra** medlemskapskällor: roller, vårdnadshavarskap, accepterade inbjudningar och **godkända ansökningar** — en godkänd ansökan ger truppens lag precis som en accepterad inbjudan. **Beslut (godkänt): inget barnfält i ansökan** — den är rent vuxen↔trupp; barnets namn (§KM.1) och samtycke (§KM.6) hör till `#196` när barnet kopplas. En väntande/godkänd ansökan per konto+trupp (dubblettspärr). Endpoints: publik `GET /api/v1/trupper/{truppId}/apply-info` (allow-listad) + `POST .../applications` (`[Authorize]`); admin-kö `GET/approve/deny` under `/api/v1/admin/trupper/{truppId}/applications` (`[AdminOfTrupp]`, objektnivå mot truppen). Nya `AuditActions` (inskickad/godkänd/nekad), id-only. **FE:** route `/ansok/$truppId` (anonym landning → logga in → ansök), återanvändbar `ApplicationsPanel` (godkänn/neka) i både trupp-adminvyn och superadmin-konsolen. 10 backend- + 3 FE-tester; backend 580 + FE 550 gröna |
| 2026-09-16 | **v2: inbjudningar — admin bjuder in vårdnadshavare, accepterad inbjudan = trupp-medlemskap** (`#193`) | En stängd, inbjudningsbaserad plattform behöver ett sätt att släppa in föräldrar. Barn-kopplingen (Guardianship) kommer först i `#196`, så "kopplas" kan inte betyda barn ännu — därför bär inbjudan själv medlemskapet | **Beslut (godkänt):** en accepterad `Invitation` **är** förälderns trupp-medlemskap (ingen separat medlemstabell), och `MembershipService` utökades så en accepterad förälder ser truppens lag redan innan barnet kopplas i `#196`. Ny **`Invitation`**-entitet + migration `AddInvitations`: token lagras bara som SHA-256 (`SessionIssuer.Hash`), 256-bit base64url, utgång (`AuthOptions.InvitationLifetime` 14d), engångs; återanvänt/utgånget token nekas och loggas på id-nivå (§KM.10). **Bunden till adressen** — accept kräver att inloggat kontos egen adress matchar inbjudans (en vidarebefordrad länk hjälper inte fel person). Ny **`AdminOfTrupp`**-policy (anspråk `admin-trupp` vs route-`truppId`, superadmin kortsluts). Endpoints under `/api/v1/admin/trupper/{truppId}/invitations` (`[AdminOfTrupp]`) + publik `GET /api/v1/invitations/{token}` (förhandsvisning, allow-listad i gästvakten) och `POST .../accept` (`[Authorize]`); `GET /api/v1/admin/my-trupper` för admin-vyn. Mejl via `IEmailSender`; `Auth:AppBaseUrl` för absolut accept-länk (ny miljövariabel, i `.env.example`). **FE:** `adminOf` i auth-context, route `/inbjudan/$token` (anonym landning) + `/admin` (trupp-adminvy), återanvändbar `InvitationsPanel` (även i superadmin-konsolen), meny-länk "Admin" för trupp-admins. **Scope-beslut:** trupp-admin-vy byggd nu; Delete/ansökningar senare. 12 backend- + 3 FE-tester; backend 570 + FE 547 gröna |
| 2026-09-16 | **v2: superadmin-CRUD för sport/klubb/trupp/lag + admin-tillsättning, med konsol** (`#192`) | Grunden för en flerklubbs-plattform: superadmin (bara du) bygger strukturen och tillsätter trupp-admins. Modulärt — nya trupper/sporter är ett par klick | Ny **`SuperAdmin`-policy** (`RequireClaim superadmin=true`) — egen anspråksgrind, inte `Admin`-rollen (den slutar vara superadmin-exklusiv när riktiga admins finns). Backend-modul `Features/Administration` (services/commands/validators/DTO/queries + `IAdministrationRepository`), endpoints under `/api/v1/admin/*`, alla `[Authorize(SuperAdmin)]` + `[RequireCsrfToken]`. **Beslut: skjut upp Delete** (kaskaderar till matcher/barn — egen issue med skyddsräcken) och **admin tilldelas ett befintligt konto via e-post** (nya personer bjuds in i `#193`). Admin-tilldelning skriver en `TeamRole{Admin, AgeGroupId}` som token-maskineriet redan gör till ett `admin-trupp`-anspråk. `AdminResult<T>`/`AdminOutcome` → precisa koder (404/409/400). Nya `AuditActions` (sport/klubb/trupp/lag + admin), allt auditat med id, aldrig PII. **FE:** `isSuperAdmin` i auth-context, route `/superadmin` (auth-grind + synlighet på anspråket), konsol med RHF+Zod-formulär för alla fyra entiteter + admin-tilldelning, meny-länk bara för superadmin. Ingen migration (entiteterna finns sedan `#190`). 16 integrationstester + FE-tester; backend 558 + FE 544 gröna |
| 2026-09-16 | **v2: appen stängd — inloggning + medlemskap krävs för allt (backend)** (`#191`) | §KM.3 v2: en öppen läsning går inte att förena med en plattform som hanterar barn och vuxna medlemskap. Object-level auktorisering på varje resurs, inte bara `[Authorize]` | **Medlemskapstjänst** `IMembershipService` (auktoritativ mot DB, inte token): superadmin ser allt, annars admin→trupp, tränare→lag, vårdnadshavare→barn-i-lag. Två nya policyer **`MemberOfTeam`** (route `slug`) och **`MemberOfMatch`** (route `matchId`/`id`), superadmin kortsluts via anspråk. Stängda: lag-lista (filtreras per medlem), lag-schema, matchdetalj, samåkningslista **och skrivningarna** (samåkning, närvaro, notisinställningar, push-prenumeration) — gäst `401`, icke-medlem `403`. **ICS-feeden borttagen** (§KM.4): `CalendarController` + hela `Application/Features/Calendar` (queries, `IcsWriter`, builder) + tester raderade. **Publik edge-cache borttagen:** `EdgeCache` reducerad till en enda regel — `private, no-store` på allt; `EdgeCacheProfile`/`EdgeCacheOptions` + opt-in-maskineriet borta. Gästvakts-testet vändes: vaktar nu att *ingen* api-endpoint saknar auktorisering utom en kort allow-lista (auth-bootstrap + cron). Testfixturer seedar medlemskap (vårdnadshavare) för att nå de stängda ytorna. **FE fick en nödvändig ändring:** `getJson` (läsvägen för lag/schema/match) skickade i v1 avsiktligt ingen token (för edge-delning) — nu bär den access-token som `getAuthJson`, annars ser en inloggad medlem `401`/tomt. E2E-sviten gjordes om till inloggade medlemmar (ICS-specen borttagen, `prepare` seedar föräldrarnas medlemskap). Backend 542 + FE 541 tester gröna, **alla CI-checks (inkl. Playwright) gröna**. **Kvar (egna issues):** FE:ns gäst-grind (route guards + "logga in"-vy — en gäst får i dag `401` utan omdirigering), borttagning av döda ICS-länkar (`CalendarLink`/`TeamCalendarLink`), och omprövning av service workerns offline-cache för nu autentiserat schema (§KM.8). Driftsätt samordnat (se *Nästa steg*) |
| 2026-09-16 | **v2 domänmodell: Sport, Child, Guardianship, roller med scope** (`#190`) | Grunden för plattformen. Behöll kodnamnen `AgeGroup` (= Trupp) och `Team` (= Lag) för att slippa döpa om dussintals filer/tester/migrationer — mappas i UI | Nya entiteter: `Sport` (fristående), `Child` (§KM.1 minimal: `FirstName` + `LastInitial`, trupp + valfritt lag), `Guardianship` (konto↔barn, många-till-många). `AgeGroup` fick `SportId`. **Rollmodell:** `TeamRole` utökad med `AgeGroupId` + ny `RoleKind.SuperAdmin`; Coach→lag, Admin→trupp, SuperAdmin→global, bevakat av CHECK-villkoret `CK_TeamRoles_ScopePassarRollen`. Guardian är relationen, inte en roll. `AccountRoles`/`RoleRepository`/JWT bär nu `IsSuperAdmin` + `AdminOf` (trupp-id) + `CoachOf` (lag-slug); `IsAdmin` behålls som kompat = superadmin tills **hård gating byggs i `#191`**. **Migration** `AddV2DomainModel` (handredigerad: skapar default-sporten Fotboll och backfiller befintliga trupper innan FK:n läggs till). **Seed:** Fotboll + superadmin via konfig `SuperAdmin__Email` (aldrig hårdkodad). Fastlåsta testlistor (arkitektur + persistens) utökade i samma PR. Delete: barn→trupp Cascade, barn→lag SetNull, guardianship→konto/barn Cascade. Testat: seed (sport/superadmin idempotent), modellen mot Npgsql, alla befintliga tester gröna |
| 2026-09-16 | **v2: stängd, inbjudningsbaserad flerklubbs-plattform — reverserar §KM.1/§KM.3/§KM.4/§KM.7** (`#189`, epic `#205`) | Produkten växer från ett öppet matchschema till en plattform: superadmin (bara ägaren) skapar sporter/klubbar/trupper/lag och tillsätter admins; admins bjuder in vårdnadshavare (och föräldrar kan ansöka), sorterar truppens ~40 barn i färg-lag (Gul/Blå/Vit/Svart), och riktar kallelser till trupp/lag/utvalda barn; chatt per trupp och per lag. Kräver barn på servern och en stängd app | **Medvetet regel-brott, väger tungt (barn under 13).** §KM omskrivet: **§KM.1** tillåter nu en *minimal* barnprofil — **förnamn + efternamnets initial ("Liam J"), aldrig hela efternamnet**, lag/trupp, vårdnadshavarkoppling; dataminimering är regeln. **§KM.2 oförändrad:** spelarkortet (resultat, mål, milstolpar + ev. sportquiz) förblir **device-only och privat** — aldrig på servern, aldrig publikt, aldrig kopplat till trupp-profilen. **§KM.3** stänger appen: inloggning **och** medlemskap för allt; **§KM.4** (publik ICS-feed) utgår; **§KM.6** kräver nu vårdnadshavarsamtycke (version + tid) innan barn kopplas; **§KM.7** låter kallelsen namnge barn (minimalt) och riktas mot målgrupp. Roller med scope: SuperAdmin/Admin/Coach/Guardian. Bygg-ordning i epic `#205` (#189→#204). Kvar att bevaka: kallstart-UX (edge-cache för publika GET försvinner, §KM.11), samtyckestextens juridiska grund, och gallring när barn lämnar |
| 2026-08-29 | **Bas = webb-PWA**, mallen `StrukturBackendFrontend` | Föräldrar installerar inte en app från App Store för ett matchschema; en länk räcker och uppdateringar slår igenom direkt | Ingen app store-avgift eller granskning. Push kräver hemskärm-installation på iOS — kalenderfeeden är fallbacken |
| 2026-08-29 | **Backend = C# / .NET** enligt mallen | Regelverkets backend-kapitel följs rakt av; kompetens och SDK finns | Driftas som Docker-container på Render, inte på Vercel — se nästa rad |
| 2026-08-29 | **Drift = Vercel + Render + Neon med `/api/*`-rewrite** | Samma uppsättning som carcheck.se, som redan kör i produktion. Noll kostnad, monorepo behålls, .NET-backenden behålls | Klienten ser **en enda origin** → CORS behöver inte öppnas och refresh-cookien blir förstapart. Priset är Renders kallstart, som måste maskeras med edge-cache (§KM.11) |
| 2026-08-29 | **Databasen på Neon, inte på Render** | Renders gratisdatabas upphör efter 30 dagar | Familjernas statistik ligger inte på något som självdör |
| 2026-08-29 | **Monorepo** i stället för två repon | Ensam utvecklare; mallen tillåter uttryckligen monorepo. En PR kan röra både BE och FE | §0 p.13 tolkas som "BE-gapet först i egen commit inom samma PR" |
| 2026-08-29 | **Steg 0 omskrivet** för ideell app | Originalfiltren mäter betalningsvilja; ingen betalar för den här appen | Grinden mäter i stället adoption. Originalfiltren finns i projektmallen, utanför repot |
| 2026-08-29 | **Full process** enligt mallen | Målet är ett komplett och granskningsbart projekt | Board, issue, branch och PR för varje ändring — även små |
| 2026-08-29 | **Statistik privat per familj**, ingen skytteliga | Svensk fotbolls riktlinjer avråder från resultatrapportering och tabeller upp till 12 år; minskar dessutom föräldrapress | Matchresultatet skrivs in av varje familj för sig. Ingen roll, inte ens Admin, kan läsa det |
| 2026-08-29 | **Barnstatistiken lagras enbart på enheten** — ingen tabell, ingen endpoint | Spelarkortet är tänkt som något föräldern och barnet gör tillsammans efter matchen. Data som aldrig når servern kan inte läcka från den, och kräver varken konto eller samtycke | Servern behandlar **inga uppgifter om barn** vid lansering. Priset: datan går förlorad vid telefonbyte utan säkerhetskopia → backupkod, `storage.persist()` och installationsuppmaning blir funktionskrav (§KM.2) |
| 2026-09-10 | **Inga barnuppgifter på servern — alls** | §KM.1 tillät tidigare förnamn och tröjnummer som förberedelse för kallelsen. Men uppgifter som aldrig lagras kan inte läcka, inte begäras ut och inte glömmas bort vid en radering — och en app för ett barnlag har inget behov som väger upp det | Ingen `Player`-entitet och ingen trupp. Kallelsen (M6) byggs om: en vuxen svarar för sin familj och anger antal, tränaren ser summan och vilka konton som inte svarat. `#40` truppvyn och `#59` samtyckesrutinen stängda som inaktuella — det finns inget att samtycka till. §KM.1, §KM.2, §KM.6 och §KM.7 ändrade i samma PR |
| 2026-09-10 | **Notisprenumerationen kopplas inte till ett konto** | Prenumerationen hör till webbläsaren och ska fungera utan inloggning — annars når notiserna en bråkdel av föräldrarna. En koppling till kontot hade dessutom mött §KM.6: allt som ägs av ett konto raderas med det, och då slutar en enhet tyst få besked om inställda matcher | `PushSubscriptions` har ingen `AccountId`. Samåkningsnotiserna i `#63` behöver kopplingen och får införa den med den avvägningen skriven — en kolumn som inte används ännu ska inte finnas |
| 2026-09-10 | **Kontot får för- och efternamn** | Servern lagrade bara en mejladress, så samåkningen blev anonym: "någon frågar om skjuts". Mellan grannar som möts på planen nästa lördag är det ett tomrum, och tränarens överblick kunde inte svara på vem som kör | Ny PII-kolumn om en **vuxen** (§KM.1:s tak gäller barn och berörs inte). Förnamn krävs, efternamn valfritt. Namnet visas bara för inloggade — aldrig i den publika erbjudandelistan, aldrig i loggar eller audit-rader, och det raderas med kontot |
| 2026-09-10 | **Gallringen körs i API-processen, inte som cron** | Vercel Hobby ger ett cronjobb per dygn, och det är lovat till påminnelsen kvällen före match (§KM.11). En gallring som beror på att en annan tjänst hör av sig slutar tyst köra den dag villkoren ändras | `CarpoolRetentionWorker` kör vid uppstart och var 24:e timme. Render free vaknar flera gånger om dagen, så den körs ofta — raderingen är idempotent och kostar en fråga när det inte finns något att ta bort |
| 2026-09-10 | **Tränarens samåkningsöverblick är räknad, inte namngiven** | Servern lagrar inget namn på en förälder, bara en mejladress — och den visas aldrig för någon annan. Överblickens fråga ("får alla skjuts?") besvaras av siffror | Ingen ny PII-exponering (§KM.1). Vill vi någon gång visa vem som kör krävs ett eget beslut här, inte en tyst utökning av svaret |
| 2026-08-29 | **Samåkning: förfrågan → accept eller nekande med meddelande** | Föraren ska själv få välja vem som åker med, och ett tyst nej fungerar inte mellan grannar som möts på planen nästa lördag | Ny entitet `CarpoolRequest` med tillståndsmaskin. Nekande utan meddelande avvisas server-side. Kräver inloggning — gäster ser men deltar inte (§KM.12) |
| 2026-08-29 | **Milstolparna omordnade till M0–M8** | Inloggningen behövs av tränaradmin och samåkning men inte längre av statistiken | Konto och roller blev en egen milstolpe (M2) före tränaradmin (M3). Spelarkortet (M4) är helt frikopplat och kan byggas parallellt |
| 2026-08-29 | **Öppen läsning, autentiserad skrivning** | En förälder som bara vill se matchtiden ska aldrig mötas av inloggning | Avvikelse §KM.0 A4. Kräver rate limiting från start och att publika endpoints aldrig returnerar PII |
| 2026-08-29 | **Kallelse byggs men aktiveras inte** | Det finns redan appar för kallelse; funktionen ska finnas den dag klubben vill byta | `Team.AttendanceEnabled` kontrolleras serverside. Ingen komplett trupp krävs vid lansering |
| 2026-08-29 | **Repot är publikt** | Ingen anledning att hålla en ideell klubbapp stängd. Beslutet togs efter att innehållet i den borttagna mallmappen granskats i detalj | Branch protection är nu gratis om den ska slås på. **Påverkar mediator-valet:** RPL-1.5 kräver källkodspublicering vid driftsättning — med publikt repo är det uppfyllt, men vår egen kod skulle då hamna under copyleft |
| 2026-08-29 | **Branch protection skjuts upp** | Ensam utvecklare, och `.githooks/pre-push` räcker | Kan slås på när som helst nu när repot är publikt |
| 2026-08-29 | **`mallar/` borttagen ur hela historiken** | Mappen innehöll en affärsplan för en orelaterad produkt och hörde inte hemma i det här repot | Historiken är omskriven och force-pushad. Se känd risk nedan om gamla objekt |
| 2026-08-29 | **M3 avblockerad** — klubbens officiella verktyg används inte, och tränarna vill ha appen | Filter 3 och Filter 4 i `SPEC.md` är därmed besvarade utan villkor. Vi ersätter en oanvänd lösning i stället för att konkurrera med en levande vana | Tränaradmin kan byggas utan förbehåll. Projektets största risk — att appen står tom — är kraftigt reducerad, men adoptionen ska ändå mätas efter lansering |
| 2026-08-30 | **Lagväljaren är länkar, inte växlingsknappar** | Sedan `#19` byter ett lagval adress, och en kontroll som byter adress ska vara en länk — annars går den inte att öppna i ny flik eller kopiera, och en skärmläsare säger "växlingsknapp" när det egentligen är "aktuell sida". `#18`:s krav på `aria-pressed` skrevs innan routingbeslutet | `aria-current="page"` ersätter `aria-pressed`. Väljaren är ett `nav`-landmärke med eget namn, vilket också gör den lätt att hoppa till eller förbi |
| 2026-08-30 | **Handskriven service worker med bara runtime-cache** | Det som normalt kräver ett verktyg är att förcacha listan över byggda filer med deras innehållshashar — och det behövs inte när filerna cachas när de hämtas. Noll nya beroenden, och varje rad går att läsa | Uppdateringsflödet är vårt ansvar, och en trasig service worker går inte att återkalla från servern. Därför körs **den fil som faktiskt levereras** i testerna, med ett låtsat scope — en kopia av logiken hade kunnat gå grön medan `public/sw.js` var trasig |
| 2026-08-30 | **Webbläsaren anropar Open-Meteo direkt, inte via vår backend** | §KM.6 tillåter uttryckligen Open-Meteo i frontenden. Att gå direkt håller Render utanför: en förälder som kollar vädret väcker då inte en sovande backend och äter inte av de 750 instanstimmarna — samma resonemang som edge-cachen i §KM.11 bygger på | Noll extra instanstimmar och ingen kallstart för vädret. Priset: förälderns IP-adress når Open-Meteo, och varje webbläsare cachar för sig i stället för centralt. Koordinaterna kommer ändå ur vår egen `Venue`-tabell via matchsvaret, aldrig ur användarindata (checklistan 4.8) |
| 2026-08-30 | **Kalenderfeeden anger tid i UTC med `Z`, inte lokal tid med `VTIMEZONE`** | Ett `Z`-suffix är entydigt och kan inte bli fel — kalenderappen räknar själv om till användarens zon. En egen `VTIMEZONE`-definition med sommartidsregler är den vanligaste orsaken till att en hemsnickrad feed visar rätt timme i en app och fel i en annan (§KM.5) | Feeden fungerar likadant i alla klienter, och skiftet i oktober sköts av kalenderappen i stället för av vår kod. En förälder utomlands ser matchen i sin egen zon, vilket är rätt för en kalenderpost — händelsen inträffar vid ett absolut ögonblick |
| 2026-08-30 | **Matchnotisen exponeras inte publikt** | Notisen är tränarens fritext, som §KM.1 räknar som potentiell PII och som inte får visas för fler än den avsedda mottagarkretsen. Publika endpoints är öppna för vem som helst med länken och cachas dessutom på Vercels edge. Tre källor pekade åt samma håll: varken `#17` eller `MVP-PLAN.md` listar notisen, och inget skriver notiser förrän tränaradmin i M3 | Fältet finns kvar i databasen men lämnar aldrig servern. Ett integrationstest matar in en notis med barnnamn och hälsouppgift och kontrollerar att den inte finns i svaret. **Frågan tas upp igen i M3**, där tränargränssnittet byggs — det är också där en varning om vad som får skrivas hör hemma |
| 2026-08-30 | **Varje lag har en egen adress: `/lag/<slug>`** | Appen sprids genom att en förälder skickar en länk i föräldragruppen (SPEC). Med schemat på startsidan hade mottagaren landat på sitt *eget* senast valda lag, eller på en lagväljare — inte på det avsändaren menade. `#21` behöver dessutom en adress per match oavsett | URL:en styr vilket lag som visas; det sparade valet avgör bara vart startsidan skickar en återvändande besökare. Omdirigeringen sker i `beforeLoad`, så väljaren aldrig blinkar förbi. Att öppna en delad länk blir också det nya ihågkomna valet |
| 2026-08-30 | **Lagfärgen visas som bricka, aldrig som textbakgrund** | Två av de fyra lagfärgerna går inte att hålla WCAG-kontrast mot som bakgrund bakom text: Vit `#D9D9D9` i ljust läge och Svart `#161616` i mörkt. Med färgen som rund bricka står texten alltid mot sidans bakgrund i stället | Kontrasten blir densamma för alla fyra lag, och accenten syns ändå tydligt i headerremsan och kortens ram. Valt lag markeras med tre signaler — `aria-pressed`, en bock och en kraftigare ram — eftersom färg inte får bära betydelsen ensam (WCAG 1.4.1) |
| 2026-08-30 | **Tidszonsregeln görs verkställbar med ESLint, och testerna körs i fel tidszon** | §KM.5 säger att konverteringen ska ske på ett ställe. En regel som bara står i ett dokument bryts förr eller senare. En ESLint-regel förbjuder `toLocale*` och egna `Intl`-formaterare utanför `src/lib/time.ts`, och hela testsviten körs i `America/Los_Angeles` | Ett tappat tidszonsargument faller i CI i stället för att upptäckas av en förälder i oktober. Utvecklarmaskinen står i Europe/Berlin, som har identiska förskjutningar med Stockholm — utan den påtvingade zonen hade sviten passerat av ren tur |
| 2026-08-30 | **Handskriven query-dispatcher i stället för MediatR** (öppen fråga 2c besvarad) | MediatR 13+ ligger under RPL-1.5, som utlöses vid **driftsättning** och skulle lägga vår egen kod under copyleft. MediatR 12.4.1 är Apache-2.0 men fryst sedan 2024. Dispatchern är ~70 rader med omslagscache och en behavior-kedja | Avvikelse **§KM.0 A9**. Noll licensyta och inget beroende som kan ändra villkor mitt i projektet. Priset: vi underhåller den själva och pipeline-behaviors byggs efter hand — därför är dispatchern täckt av egna tester som bevisar uppslagning, ordning och att valideringen avbryter |
| 2026-08-29 | **Uppetidspingen fönstras i stället för att gå dygnet runt** | Render free ger 750 instanstimmar per månad och arbetsyta, och suspenderar **alla** fria tjänster resten av månaden när de tar slut. En ping var femte minut dygnet runt förbrukar 744 timmar i en 31-dagarsmånad — sex timmars marginal. Det är ett dåligt byte för en app hundra familjer förlitar sig på | Pingas var 5:e minut fredag 15:00 till söndag 22:59, plus en daglig kontroll 14:50. ~251 h/månad, ~499 h marginal. Priset: en tränare som redigerar en vardagskväll kan möta ~1 minuts kallstart, och avbrott utanför helgen upptäcks inom ett dygn. Rutinen står i `docs/DRIFTOVERVAKNING.md` |
| 2026-08-29 | **Databasdumpar tas manuellt, aldrig i CI** | En schemalagd GitHub Actions-körning hade varit lätt att skriva, men artefakter i ett publikt repo går att ladda ner av vem som helst med länken. Så snart en tränare lägger upp truppen ligger barns förnamn i databasen (§KM.1) | Neons PITR är det automatiska lagret. Den logiska dumpen tas av en människa, till en katalog utanför repot. `.gitignore` blockerar `*.dump` som skyddsnät |
| 2026-08-29 | **Cachning är opt-in, inte opt-out** | Standarden för varje svar är `private, no-store`; en endpoint blir publikt cachebar först genom att uttryckligen säga det. Motsatt standard hade gjort en glömd markering till ett dataläckage i stället för till en missad optimering | Varje publik endpoint i M1 måste komma ihåg `.WithEdgeCache(...)`, annars väcks Render i onödan. Priset är medvetet: att glömma kostar prestanda, aldrig integritet |
| 2026-08-29 | **Arkitekturtesterna skrivs för hand — NetArchTest väljs bort** | Paketet som issue #10 namnger har inte släppts sedan maj 2021 och **deklarerar ingen licens i paketmetadatan** (GitHub-repot uppger MIT, nuspec:en är tom). Efter MediatR-överraskningen är ett olicensierat, övergivet beroende inte värt bekvämligheten. Alternativen — eNhancedEdition (MIT) och ArchUnitNET (Apache-2.0) — hade fungerat, men behövdes inte | Noll ny licensyta. De befintliga handskrivna reglerna täckte redan två av issuets tre krav; det tredje blev ~60 rader reflektion. Priset: vi underhåller detektorerna själva, vilket motiverar självtesterna som bevisar att varje regel faktiskt faller |
| 2026-08-30 | **E-post för inloggningskoden = Resend**, trots att den lagrar loggar i USA | Underlaget rekommenderade Scaleway (franska servrar, ingen reklamfot, ~0 kr), men det kräver betalkort och legitimering. De EU-baserade gratisnivåerna — Brevo och Mailjet — stämplar sin egen reklamfot även i transaktionsmejl, och ett inloggningsmejl med främmande reklam är precis vad föräldrar lärt sig att inte klicka på. **Det enda som når leverantören är en vuxens mejladress** — barnuppgifter kan inte hamna där, eftersom de aldrig når någon server (§KM.2) | Checklistans rad 9.12 får ett **dokumenterat undantag** i stället för att tyst brytas, och ny rad 9.14 kräver biträdesavtal, kontrollerad överföringsgrund och namngivet undantag i integritetstexten. Två följder att bevaka: gratisnivån går vid **100 mejl per dygn**, vilket en lanseringskväll med hundra föräldrar ligger på gränsen till — bjud in i omgångar. Och överföringen vilar på DPF, se känd risk nedan |
| 2026-09-02 | **Spelarkortets schema gick till version 2 — första migreringen** | Matchrapporten behövde resultatfält, som inte fanns i version 1. Migreringskedjan från `#42` fick därmed sitt första riktiga steg | Gamla rapporter får **`null`, inte `0`**. Vi vet inte vad de matcherna slutade, och en nolla hade sett ut som ett svar: en familj som öppnar en gammal match skulle läsa 0–0 som något de själva skrivit |
| 2026-09-02 | **Spelarkortets lagring importerar inte API-lagret, och ett test vaktar det** | §KM.2 säger att kortet aldrig når servern. Ett beteendetest fångar bara de anrop testet råkar utlösa; en saknad import syns oavsett vem som anropar vad | Vägen till ett nätverksanrop finns helt enkelt inte i de filerna. Det är den starkaste formen av garantin: inte att vi låter bli, utan att det inte går |
| 2026-09-02 | **Beständig lagring begärs först när kortet får innehåll, inte vid start** | De allra flesta öppnar appen för att se en matchtid och rör aldrig spelarkortet. En ruta för något man inte gör besvaras med nej — och då är skyddet sämre för dem som sedan börjar använda kortet | Svaret ignoreras med flit: appen fungerar likadant utan beständig lagring, den är bara mer utsatt. Det starkaste skyddet är installation på hemskärmen (`#48`) och säkerhetskopian (`#47`), inte den här begäran |
| 2026-09-02 | **Krockgränsen är två timmar, satt mot klubbens verkliga schema** | En gräns som larmar på ett normalt schema blir avstängd, inte fixad. I klubbens riktiga schema ligger fyra lag 75 minuter isär på samma plan — det är planering, inte en krock | Två matcher för **samma lag** närmare än två timmar är däremot ett misstag: laget kan inte spela båda. Inställda matcher räknas inte, eftersom de inte tar någon tid i anspråk. Markeringen står på raden och är ett ord, inte en färg |
| 2026-09-02 | **Importen tolkar om texten i stället för att lita på granskningen** | Skulle servern spara det klienten säger att texten betydde vore förhandsgranskningen en rekommendation. En tränare kunde då skicka in vad som helst som "det parsern kom fram till" | Klienten skickar samma inklistring en gång till, aldrig den tolkade listan. Samma princip som att spelplatsens koordinater aldrig kommer från klienten |
| 2026-09-02 | **Bara det egna lagets rader importeras** | Ett inklistrat serieschema innehåller ofta alla fyra lagen, och behörigheten gäller ett. Utan kontrollen kunde en tränare för Gul lägga upp matcher i Blå genom att klistra in hela serien — förbi policyn, som bara sett på slugen i adressen | Raderna rapporteras som `OtherTeam` och hoppas över i stället för att tyst försvinna: tränaren ska förstå varför tjugofem rader blev sex |
| 2026-09-02 | **Enum-värden serialiseras som text i API:t** | En siffra tvingar klienten att känna till enumens ordning, och att lägga till ett värde i mitten ändrar då tyst betydelsen av alla svar som redan skickats | Upptäcktes när radernas utfall kom ut som `0`, `1`, `2`. Påverkar inga befintliga svar — `MatchDto.Status` var redan en sträng |
| 2026-09-02 | **Massinläggets parser ger besked per rad, inte per inklistring** | Att knappa in tjugofem matcher för hand gör ingen två gånger, och en app utan matcher är en app ingen öppnar igen. Men en parser som svarar "något gick fel" är nästan lika illa: tränaren vet inte vilken rad som är trasig | Varje rad får ett eget utfall — klar, ofullständig, otolkbart datum, okänt lag eller spelplats, eller dubblett — och resten tolkas ändå. **Tabb prövas före komma**, eftersom lagnamn innehåller komma ("Kareby IS, Blå") men aldrig tabb. Tak på 500 rader, för att en inklistring på tio megabyte inte ska binda upp den enda Render-instansen |
| 2026-09-02 | **Svensk lokaltid till UTC räknas om i `lib/time.ts`, inte i formuläret** | §KM.5 kräver att konverteringen sker på ett enda ställe i frontenden. `new Date('2026-09-20T14:00')` tolkas i webbläsarens zon — en tränare som lägger in en match från en semester i Spanien hade fått den sparad en timme fel, och felet syns först i föräldrarnas kalendrar | Omräkningen mäter offseten vid det gissade ögonblicket och korrigerar **två gånger**. Andra rundan behövs för timmen före vårskiftet, då första gissningen hamnar på fel sida om omställningen. Tvetydiga tider (02:00 på höstskiftet, som inträffar två gånger) löses till det **tidigare** ögonblicket, och tider som inte finns avvisas |
| 2026-09-02 | **Vid krock i en import vinner den lokala telefonen** | Import ska aldrig kunna göra skada. Den telefon man står med är den man valt att återställa *till*, och det som skrivs över finns ingen annanstans — koden går alltid att importera om, men den överskrivna statistiken gör det inte | Importen **lägger till**, den ersätter aldrig. Två vårdnadshavare med varsin telefon har varsin uppsättning statistik (§KM.2); den som läser in den andres kod får det som saknas och behåller sitt eget. Användaren får veta exakt vad som tillkom |
| 2026-09-02 | **Föregångarens `KARRA1.`-kod läses, och formatet är avläst — inte antaget** | Familjerna har en säsong i den gamla appen. En gissad form hade inte fallit förrän någon försökte flytta över på riktigt och misslyckades | Formen lästes ur `index 4.html`: `{ kids: [{id,name,team}], stats: { "datum_lag_motståndare": { us, them, kids: { id: {g,a} } } } }`. Datumet räddas ur statistikens nyckel; matchens id finns inte, eftersom den gamla appen inte hade några. Rapporterna kommer alltså in utan koppling till en match — **men med sin statistik i behåll**, vilket är det familjen ville flytta med sig |
| 2026-09-02 | **Base64 går via `TextEncoder`, inte rakt genom `btoa`** | `btoa` klarar bara latin-1. Ett barn som heter Åsa hade fått koden att kasta — och det är ett namn som finns i vilket lag som helst | Samma omväg som föregångaren använde, vilket också är varför koderna går att läsa åt båda håll. Ett test med `Åsa Ärlig Öberg` vaktar det |
| 2026-09-03 | **De sex märkena är avlästa ur föregångaren, inte påhittade** | Familjerna har redan en säsong bakom sig. Ett barn som låst upp Hattrick ska inte behöva göra om det för att vi tyckte att fyra mål lät bättre — och tonen fanns redan där, den behövde flyttas, inte uppfinnas | Samma emoji, samma namn, samma trösklar som `index 4.html`: Första målet, Passningskung (5 assist), Hattrick (3 mål i **en** match), Målmaskin (10 mål), Poängstjärna (10 mål och assist ihop), Stammis (10 matcher). Även visningsordningen är föregångarens — den är inte alfabetisk och inte efter svårighet, utan den familjerna känner igen |
| 2026-09-03 | **Schemat gick till version 3 — barnet minns vilka märken det sett** | Ett firande som återkommer vid varje omstart är ingen händelse utan en påminnelse man klickar bort. Märkena själva sparas aldrig, bara **vad barnet sett** — de räknas fram ur statistiken varje gång, så en ändrad matchrapport ger ett ändrat märke | Migreringen 2 → 3 markerar allt redan förtjänat som sett: en familj med en hel säsong ska inte mötas av sex firanden vid en uppdatering, som firar ingenting som just hänt och dränker det första riktiga. Samma val görs vid import av en `KARRA1.`-kod, av samma skäl. Märkena syns ändå direkt i listan som upplåsta — det är firandet som är reserverat för ögonblicket, inte märket |
| 2026-09-03 | **Säkerhetskopior går genom migreringskedjan — hittat när schemat ändrades** | En kod bär sin egen version och kan vara äldre än telefonen som läser den. En kod sparad förra säsongen är precis det som ska gå att läsa | `decodeBackup` gick förbi kedjan och lämnade kortet till `writeCard`, som stämplar aktuell version på allt den skriver. En version 2-kod hade alltså skrivits ner som version 3 utan att vara det, och det saknade fältet upptäckts först när något läste det. Fångat i samma PR som införde version 3 — nästa schemaändring hade annars gått samma väg |
| 2026-09-03 | **Låst märke bärs av text och ram, inte av ljushet — och rörelsen är opt-in** | Föregångaren dämpade låsta märken med `opacity: .4` och inget annat. Det gör ljusheten till enda bäraren av beskedet (WCAG 1.4.1) och lägger kontrasten under 4,5:1 (§KM.0 A3) | Låsta märken har full läskontrast, ordet "låst" som en hel mening för skärmläsaren, och en mätare som säger hur långt barnet kommit — det är de låsta som ger något att sikta på. Firandets animation ligger bakom `prefers-reduced-motion: no-preference` i stället för att nollas av skyddsnätet längst ned i stilmallen: rörelse ska vara tillagd, inte bortstädad |
| 2026-09-03 | **Rapporten skriver av motståndarens namn — schema version 4** | Barnets egen sida ska gå att läsa på ett flygplan, på en telefon utan valt lag, och för en familj som just importerat sin säsong från den gamla appen. Att slå upp motståndaren i lagets schema hade krävt nät — och öppnat en väg ut från just de filer där §KM.2 säger att ingen sådan väg ska finnas | Lagnamn är inte personuppgifter; de står redan i det publika schemat och i ICS-feeden. Namnet skrivs av vid **varje** sparning, inte bara när rapporten skapas, så en rapport som fylldes i på version 3 får namnet vid nästa tryck. Rapporter som aldrig rörs igen visar datumet ensamt i stället för ett påhittat lagnamn |
| 2026-09-03 | **Motståndaren räddas ur föregångarens statistiknyckel** | Nyckeln är `datum_lag_motståndare`. En importerad säsong behöver alltså inte tappa vem matcherna spelades mot — informationen fanns där hela tiden | Nyckelns tredje del och framåt fogas ihop igen, ifall ett lagnamn skulle innehålla ett understreck: en avhuggen motståndare vore sämre än ingen alls |
| 2026-09-03 | **Ett halvt resultat räknas inte in i facit** | Ett ifyllt "våra mål" utan "deras mål" säger ingenting om utgången. Att läsa det tomma fältet som noll hade hittat på en vinst som familjen aldrig skrivit | Samma hållning som schemat version 2 tog till gamla rapporter: `null` betyder *vi vet inte*, aldrig noll. Facit visar bara matcher där båda målen är ifyllda, och säsongstexten säger uttryckligen "av de N med ifyllt resultat" |
| 2026-09-03 | **Säsongen sägs i meningar, och inga nollor skrivs ut** | Siffrorna står redan i totalerna. Det som gör sidan till ett samlarkort i stället för ett kalkylark är att någon säger vad de betyder — och en nolla ser ut som ett omdöme | Den som bara har assist får läsa om sina assist, inte "0 mål och 2 assist". Bästa matchen nämns först vid två mål: ett mål i en säsong där barnet gjort ett mål per match är ingen höjdpunkt, det är säsongen. Genitiv följer svensk regel — "Elias matcher", inte "Eliass matcher", vilket hade synts direkt i den här åldersgruppen |
| 2026-09-03 | **Installationstipset visas bara på iOS, bara med innehåll, och bara en gång** | iOS rensar lagring för webbplatser som inte besökts på sju dagar; appar på hemskärmen är undantagna. Appen används säsongsvis, så en förälder med bara ett bokmärke kan förlora en hel säsong över sommaruppehållet utan att ha gjort något fel | Fyra villkor måste gälla samtidigt: det är iOS, appen ligger inte redan på hemskärmen, tipset har inte stängts, och kortet har innehåll. Samma resonemang som bakom `persist()`-frågan — ett råd om något man inte gör är precis det som lär folk att sluta läsa appens texter |
| 2026-09-03 | **Rådet om hemskärmen står också permanent, inte bara i den bortklickbara rutan** | Den som stänger tipset i våras ska kunna hitta skälet igen till hösten, och det är just över sommaruppehållet som iOS hinner rensa | Rutan är påminnelsen, texten under "Vad betyder det?" är uppslagsverket. Där står också vad som får statistiken att försvinna — telefonbyte, rensad webbläsardata, privat fönster — och att två telefoner i familjen får varsin uppsättning |
| 2026-09-03 | **iOS känns igen på user agent, vilket annars är fel väg** | iPadOS 13 och senare uppger sig vara en Mac, så en iPad känns igen på att den har pekskärm. Det går inte att ersätta med en förmågekontroll: det som skiljer är inte en förmåga utan en **lagringspolicy**, och den syns inte i något API | Kontrollen är testad åt båda hållen — en iPad som utger sig för att vara en Mac ska få tipset, en riktig Mac ska inte. `isInstalled()` läser både `display-mode: standalone` och iOS Safaris egen `navigator.standalone`, och svarar nej i stället för att kasta när `matchMedia` saknas |
| 2026-09-03 | **§KM.2-vakten granskar nu migrationer och kolumner, inte bara namn** | De befintliga testerna utgick från att en ny yta föds med ett nytt *namn* — en `PlayerStatsDto`, en `/stats`-route. Så går det sällan till. Det troliga är att någon lägger en kolumn `Goals` på en tabell som redan finns, eller skriver en migration som skapar tabellen utan att en entitet någonsin dyker upp i `DbContext`. Ingen av de gamla reglerna hade sagt ett ord | Checklistans rad 2.4 påstod att migrationer var verifierade; de var de inte. Raden var alltså grön för något som inte kontrollerades — vilket är sämre än en tom ruta, eftersom den intalar att frågan är avklarad |
| 2026-09-03 | **Migrationerna läses som operationer, inte som text** | En textsökning över migrationsfilerna missar `Sql("CREATE TABLE ...")` lika lätt som den larmar på ett kommentarsord. Operationerna är vad som faktiskt körs mot databasen | `Migration.UpOperations` utvecklas och granskas per operation: tabellnamn, kolumnnamn, index, sekvenser och byten av namn. Rå SQL är den enda operation vars innehåll inte går att läsa som ett namn, så bara den granskas som text. Två påhittade migrationer i testfilen bevisar att avläsningen ser en tabell och en kolumn som §KM.2 förbjuder |
| 2026-09-03 | **Ny tabell: `CarpoolOffers`** | Samåkningen behöver en plats på servern — det är den enda funktionen där föräldrar gör något med varandra, och den kan inte ligga på en enskild telefon | Tabellen bär en **vuxens** erbjudande: riktning, avgångsplats, avgångstid, antal platser och förarens egen notis. Inget barn nämns, inget namn och inget telefonnummer (§KM.1). Främmande nyckel till `Accounts` med kaskad, så att en kontoradering tar med sig det kontot lagt upp (§KM.6). Gallras 30 dagar efter matchen (§KM.12) — den delen byggs i `#55` |
| 2026-09-03 | **"Fullt" lagras inte, det räknas fram** | §KM.12 beskriver kedjan Öppet → Fullt → Tillbakadraget, men bara accepterade förfrågningar förbrukar platser. En sparad "fullt"-flagga hade varit ett andra ställe som vet hur många platser som är kvar, och det stället hade förr eller senare sagt något annat än förfrågningarna | Entiteten har bara `Open` och `Withdrawn`. Det fel som annars uppstår är det värsta i den här funktionen: någon står kvar på parkeringen |
| 2026-09-03 | **Notisen lämnas ute ur svaret till en gäst, och listan är aldrig edge-cachad** | §KM.3 gör erbjudandena publika, §KM.12 säger att fritext bara får nå de inblandade. Båda kan gälla samtidigt om svaret innehåller olika mycket beroende på vem som frågar | Men då får svaret **aldrig** ligga i en delad cache — annars levereras en förälders fritext till någon annan från Vercels edge. Endpointen är därför medvetet inte märkt med `WithEdgeCache` och får `private`. Kallstarten på Render får kosta här, och ett test vaktar att ingen "optimerar" bort det |
| 2026-09-03 | **Läsning och skrivning ligger i varsin controller, inte i en med `[AllowAnonymous]`** | Blandat i en klass fungerar det vid körning, men den publika läsningen bär då auktoriseringsmetadata — och gästvakten från `#33` kan inte längre skilja en öppen endpoint från en som råkat bli stängd. Vakten fällde bygget direkt när jag försökte | Samma uppdelning som `MatchesController`/`MatchAdminController` redan använder, och vars kommentar varnar för precis det här |
| 2026-09-03 | **Föraren identifierar sig i notisen — inget namnfält på kontot** | Kontot lagrar bara en mejladress, som aldrig visas, så ett erbjudande kan inte säga vem som kör. Beslutat med Haval 2026-09-03: notisen får bära det | Samma hållning som §KM.12 har till telefonnummer — appen frågar aldrig, men den förälder som vill skriva "Johan, Elias pappa" får göra det. Ingen ny PII-kolumn införs, och notisen omfattas av gallringen efter 30 dagar. Gäller `#53` när samåkningsvyn byggs |
| 2026-09-07 | **Appen fick en huvudmeny — fyra milstolpar var osynliga** | `RootLayout` bestod av en hopplänk och en `<Outlet />`. Spelarkortet, inloggningen, kontosidan och tränarvyn låg i drift utan att gå att nå annat än genom att skriva adressen. Varje milstolpe byggde sin sida och antog att någon annan kopplade ihop dem — och bland 73 issues fanns inget om navigation | Menyn ligger i `RootLayout`, efter hopplänken så att den går att hoppa förbi. Den *visar* och tillåter inget: tränarlänken syns för den som är tränare, men auktoriseringen ligger kvar i backend. Inget alternativ visas medan sessionen förnyas — annars blinkar "Logga in" förbi för varje inloggad förälder. Lärdomen är större än menyn: en färdig milstolpe är inte färdig förrän den går att nå |
| 2026-09-07 | **Ny kolumn: `CarpoolRequests.ResponseMessage`** | Ett nekande kräver ett meddelande (§KM.12), och det måste gå att läsa för den som frågade — annars är kravet bara en formulärkontroll | Svaret ligger på förfrågan och inte på erbjudandet: det gäller **en** familj, och ett nej till grannen är inget de andra ska läsa. Samma tak som hälsningen, 500 tecken. Fritext, alltså aldrig i audit-loggen och omfattad av gallringen efter 30 dagar |
| 2026-09-07 | **Ett svar ges en gång — till skillnad från ett återtagande** | Att återta något som redan är återtaget är ofarligt och svarar ja. Ett andra *svar* betyder att föraren ändrar sig, och det är inte samma händelse: en familj som fått ja och packat bilen ska inte kunna få det omgjort till nej med en knapptryckning | Andra svaret ger `409`. Den som ändrat sig får göra det med orden, i meddelandefältet. `#55` får avgöra om en förare ska kunna ta tillbaka ett ja, och i så fall vad den som drabbas får veta |
| 2026-09-07 | **Platsräkningen frågar databasen, den litar inte på erbjudandet** | Följdbeslutet till att "fullt" räknas fram (2026-09-03): accepten måste läsa summan av de accepterade förfrågningarna, inte ett tal på erbjudandet | En accept som spränger antalet avvisas med `409` och ett fel som säger hur många platser som faktiskt är kvar — aldrig genom att knappen döljs, för en dold knapp är ingen regel. Kvarstående risk: två accepter i samma sekund kan sälja en plats för mycket, eftersom raden inte låses. Föraren är en person med en telefon, så det får stå kvar tills något visar motsatsen |
| 2026-09-03 | **Ny tabell: `CarpoolRequests`, med ett filtrerat unikt index** | En aktiv förfrågan per person och erbjudande. Kontrollen i handlern ger det begripliga felet, men två anrop som kommer samtidigt hinner båda läsa "ingen aktiv finns" innan någon skrivit — då är det bara databasen som kan säga nej | Filtret är `Status IN ('Pending','Accepted')`: en nekad eller återtagen förfrågan blockerar inte, eftersom planerna kan ha ändrats och att låsa ute någon för att de frågat en gång vore fel |
| 2026-09-03 | **Indexet vaktas i modelltesterna, inte i integrationstesterna** | Integrationstesterna kör mot EF:s InMemory-provider, som varken bryr sig om unika index eller filter. Ett test där hade gått grönt vad än indexet sa — **värre än inget test alls** | Upptäckt genom att provet inte föll: `Assert.ThrowsAny<DbUpdateException>` gav "No exception was thrown" fastän indexet fanns. Kontrollen ligger nu i `PersistenceModelTests`, som bygger modellen mot Npgsql utan att en databas behöver finnas. Detsamma gäller varje framtida databasgaranti |
| 2026-09-03 | **Gästvakten fick en spegelbild: `IsAuthenticatedByDesign`** | Åkförfrågningarna ligger under `api/v1/matches/...`, som vakten räknar som publik läsyta — men de är något annat än schemat: en sak mellan två föräldrar, där hälsningen är fritext som bara får nå de inblandade (§KM.12) | Undantaget är namngivet och jämförs exakt, inte på prefix, precis som `IsAnonymousByDesign` för skrivningar. Listan ska förbli kort: varje rad är ett undantag någon måste kunna försvara. Erbjudandena är fortsatt öppna för alla |
| 2026-09-06 | **Varje obligatorisk hemlighet listas i `render.yaml` med `sync: false`** | Blueprinten nämnde bara `ConnectionStrings__Default`. PR `#30` gjorde `Auth__SigningKey` och `Email__ApiKey` obligatoriska vid uppstart (`ValidateOnStart` respektive ett kast i `AddEmail`) utan att någondera skrevs in där, och ingen satte dem i dashboarden. Från 30 augusti till 6 september föll varje driftsättning på uppstarten — men Render behöll den gamla friska containern, så API:t svarade hela tiden med kod från 30 augusti. Felet syntes inte i CI, inte i appen och inte på `/health` | Blueprinten är nu listan över vad som måste sättas: en tom ruta i Renders dashboard i stället för ett fel gömt i en logg. Priset är att varje ny obligatorisk hemlighet måste skrivas in på två ställen — `backend/.env.example` och `render.yaml` — vilket är hela poängen |
| 2026-09-01 | **Ny tredjepart: Nominatim (OpenStreetMap) för adressuppslagning** | §KM.6 kräver ett skrivet beslut för varje ny tredjepart. Koordinater måste härledas ur adressen — de sju handinmatade låg upp till 2,2 km fel, och vid kusten är två kilometer skillnaden mellan regn och uppehåll i prognosen | **Server-side, aldrig från frontenden**, så §KM.6:s förbud mot externa skript i klienten är orört. Anropet sker **en gång per spelplats, vid sparande** — aldrig vid läsning — vilket ger en handfull anrop per säsong och ligger med god marginal inom villkoret om högst en förfrågan per sekund. Värden är fast och adressen går in som kodad frågeparameter (SSRF, checklistan 4.8). `User-Agent` identifierar oss, vilket villkoren kräver |
| 2026-09-01 | **Klienten skickar aldrig koordinater — inte ens i bekräftelsesteget** | Tränaren ska få bekräfta träffen, men om klienten skickar tillbaka en position blir geokodningen en rekommendation i stället för en regel | En adress med flera träffar besvaras med **kandidaternas adresser**, och tränaren skickar tillbaka den hen valde. Servern slår upp den på nytt. `VenueRequest` har inga koordinatfält alls |
| 2026-09-01 | **Laget står i adressen, inte i indata** | Behörigheten prövas mot lagets slug i routen (`CoachOfTeam`). Toge kommandot emot ett lag-id i kroppen kunde en tränare för Gul skicka Blås id och kringgå kontrollen helt | `MatchRequest` har inget lagfält. Dessutom kontrolleras att **matchen** hör till laget — policyn har bara sett på slugen, så utan den kontrollen kunde ett match-id från ett annat lag skickas till den egna lagadressen. Svaret är 404 och inte 403, så det inte går att kartlägga andra lags matcher genom att prova |
| 2026-09-01 | **Gästvakten mäter nu metod, inte bara sökväg** | Tränarens endpoints ligger på `api/v1/teams/{slug}/matches` — samma adress som den publika läsningen, men med POST, PUT och DELETE. Vakten från `#34` matchade på sökväg och fällde därför bygget | Den hade rätt att titta men fel upplösning. §KM.3 är *publik läsning, autentiserad skrivning*: säkra metoder måste vara öppna, och **allt som skriver måste vara skyddat**. Båda riktningarna vaktas nu, och den omvända fångar en oskyddad POST — vilket är lättare att skriva av misstag än en skyddad GET |
| 2026-09-01 | **Anti-forgery-token är bunden till identiteten — klienten måste hämta en ny vid inloggning** | ASP.NET binder token till den inloggade användaren. En token hämtad utloggad gäller därför **inte** för ett inloggat anrop. Det är avsiktligt och bra: en stulen CSRF-token blir oanvändbar för någon annans session | **Upptäckt genom att kontoraderingen svarade 400 i integrationstesterna** — och det hade slagit till likadant i drift, på varje anrop som ändrar något efter en inloggning. Klienten nollar nu CSRF-cachen när sessionen ändras, via en signal från `session.ts` |
| 2026-09-11 | **Nollningen räckte inte — CSRF-token måste hämtas *med* access-token** | Att nolla cachen (raden ovan) fick klienten att hämta en **ny** token efter inloggning, men hämtningen gick via `getJson`, som inte skickar `Authorization`. Den nya token blev därför lika anonymt bunden som den gamla, och varje inloggad skrivning svarade 400. Integrationstesterna missade det: testklienten autentiserar även `/auth/csrf`, så token blev identitetsbunden där — samma test-mot-drift-glapp som Render-migrationerna | Upptäckt i drift: *"Erbjudandet gick inte att spara"* när en inloggad förälder la upp skjuts. `getCsrfToken` använder nu `getAuthJson`, som bär access-token, så token binds till rätt identitet. Reproducerat mot en lokal container: anonymt hämtad token → 400, identitetsbunden → 201. Ett enhetstest vaktar att `/auth/csrf`-anropet bär rubriken när en session finns. Berörde **allt** som skriver inloggat — samåkning, svar på förfrågan, spara namn, radera konto |
| 2026-09-11 | **Nya tabeller: `AttendanceCalls` och `AttendanceResponses` — utan en enda barnuppgift** | Kallelsen (`#57`) behöver en plats på servern, men beslutet 2026-09-10 säger att inga barnuppgifter lagras där. Lösningen: en vuxen svarar för sin familj med ett **antal**, aldrig ett namn | `AttendanceCall` bär matchens id och när den öppnades; `AttendanceResponse` bär status (Kommer/Kan inte/Kanske) och ett tal 0–4. Ingen `Player`, ingen namnkolumn. Svaret kaskaderar bort med kontot (§KM.6) och med matchen; kallelsens `OpenedByAccountId` har ingen FK och överlever en kontoradering, som audit-raden. Ett unikt index per match (kallelse) och per konto+match (svar) ger idempotensen som handlern lovar. Arkitekturtestets tabell-lista utökades i samma PR — den grinden är hela poängen. Allt bakom `AttendanceGate` (§KM.7): `404` med flaggan av |
| 2026-09-11 | **`#58` delades: summeringen byggd, "inte svarat" + påminnelse uppskjutet till `#63`** | Tränaren ska se vilka som *inte* svarat och påminna dem — men det kräver en lista på vilka som *förväntas* svara, alltså en förälder↔lag-koppling. Den finns inte med flit: servern lagrar inget om vilka familjer som hör till ett lag (§KM.1, beslut 2026-09-10), och push-prenumerationerna är anonyma utan kontokoppling (den införs först i `#63`, enligt `PushSubscription`) | Summeringen räknar bara dem som **svarat**: `ComingPeople` (summan av antalen från Kommer), `MaybePeople`, `CantComeFamilies`, plus de svarande vuxnas namn (`#154`). Det är den siffra tränaren räknar mot elva, och den svarar på issuets *Varför*. "Inte svarat"-listan och påminnelsen hör hemma i `#63` där kopplingen ändå byggs — att gissa en mottagarkrets nu vore fel. Coach-only GET, aldrig cachad (bär namn), bakom grinden. Tillagd i gästvaktens `IsAuthenticatedByDesign` |
| 2026-09-14 | **Push-prenumerationen fick en valfri kontokoppling — `SetNull`, inte cascade** (`#63`) | Samåkningsnotiser måste nå en <b>bestämd</b> förälder (föraren, den som frågade), inte bara ett helt lag. Det kräver att en prenumeration vet vilket konto som satt bakom webbläsaren — kopplingen `PushSubscription` sköt upp till hit. `PushDispatch` fick därför en mottagarkrets: <em>ett lag</em> (`ToTeam`) eller <em>bestämda konton</em> (`ToAccounts`) | Nullbart `AccountId` på `PushSubscription`, satt vid prenumeration om anropet bär en bearer (endpointen är fortsatt öppen, §KM.3 — en gäst ger null). **FK:n är `OnDelete: SetNull`, inte cascade** (beslut 2026-09-14): en prenumeration hör till enheten, inte kontot — raderas kontot släpper kopplingen men enheten får kvar lagets matchnotiser. Det kontot ägde (vem-är-vem) försvinner ändå, vilket §KM.6 kräver. `DeleteAccountTests`-vakten fick ett namngivet undantag för just den FK:n. **Att göra när FE-prenumerationen byggs:** anropet måste gå via den autentiserade API-klienten (`postJson`), annars knyts aldrig kontot — det finns ingen FE-prenumeration än |
| 2026-09-15 | **Notisinställningar per konto, lag och typ — filtrerade vid utskick** (`#65`) | En förälder med barn i två lag ska inte tvingas få allt, och den som bara vill ha matchändringar ska slippa samåkning. Tre grovkorniga typer: `Matchändringar`, `Samåkning`, `Påminnelser` | Ny tabell `NotificationPreferences(AccountId, TeamId, tre bools)`, unik per konto+lag, kaskad. **Frånvaro av rad = allt på** — förvalet, och en läsning skapar ingen rad. `PushDispatch` fick en `PushCategory` och bär nu **alltid** ett lag (även kontoriktade notiser, så filtreringen kan ske per lag). Delivery-repot utesluter mottagare vars konto stängt av kategorin; en **anonym** prenumerant utan konto har inga inställningar och får allt som förr. Avstängning gäller vid nästa utskick — server-side, inte dolt i FE. Inställnings-API under `teams/{slug}/notification-settings` (inloggad, i gästvaktens `IsAuthenticatedByDesign`). FE: tre växlar på lagsidan + **iOS-installationstips** (§KM.8: push kräver hemskärm). Testat: CRUD, och att en avstängd kategori faktiskt utesluts medan andra når fram |
| 2026-09-15 | **Kvällspåminnelsen: en Vercel-cron väcker en skyddad backend-endpoint** (`#64`) | Render sover efter en kvart, och kvällen före match är en sådan tyst stund — en timer i processen skulle inte vakna. Därför en extern trigger: Vercels dygnscron (`0 18 * * *` i `vercel.json`) anropar `POST /api/v1/jobs/match-reminders`, som väcker Render och kör jobbet (§KM.11) | **Skydd:** en delad hemlighet i `Authorization: Bearer`, jämförd med konstant tid mot `Jobs:Secret` (satt som `Jobs__Secret` i Render, `CRON_SECRET` i Vercel; rewriten skickar headern vidare). Tom hemlighet **stänger** jobbet (avvisar allt), välter inte uppstarten. Undantaget i gästvaktens `IsAnonymousByDesign`. **Idempotens:** ny nullbar `Match.ReminderSentUtc` — bara omärkta matcher påminns, sedan märks de; en andra körning hittar inga. **"I morgon"** = morgondagens svenska datum, omräknat till ett UTC-fönster via `SwedishTime` (§KM.5). Inställda matcher tas bort med flit. Testat med fast klocka: skydd, rätt fönster, inställda uteslutna, dubbelkörning ger inga dubbletter. **Att göra i drift:** sätt `Jobs__Secret` i Render och `CRON_SECRET` i Vercel till samma värde |
| 2026-09-15 | **`#58` slutförd: "inte svarat" och påminnelse, mätt mot lagets prenumeranter** | Den uppskjutna delen (`#58` delades 2026-09-11) — tränaren ser vilka som inte svarat och kan påminna dem. Kräver en lista på vilka som *förväntas* svara, som `#63`:s konto↔prenumeration-koppling nu ger | "Inte svarat" = lagets prenumeranter **med konto** minus dem som svarat. Det är den enda konto-baserade lag-kopplingen, och exakt den mängd som kan ta emot en push. En gäst utan konto räknas inte (kan varken svara som konto eller nås av en notis). Summeringen bär antalet + namnen på dem som fyllt i ett (`#154`); den utan namn syns bara i antalet. Påminnelse-endpointen (tränare, bakom grinden) pushar `ToAccounts` till just de icke-svarande — aldrig till någon som svarat, ingen fritext. Testat: summeringen räknar rätt, påminnelsen når bara rätt mängd, och svaret säger hur många som påmindes (aldrig vilka) |
| 2026-09-16 | **Föräldrainstruktion skriven inför utrullning** (`#73`, delvis) | Kalenderprenumerationen är appens mest värdefulla och minst självklara funktion; utan en instruktion hittar föräldrarna den inte | `docs/FORALDRAINSTRUKTION.md` — färdig att klistra in i föräldrachatten, i kort och lång version, med kalenderprenumerationen som första steg (iOS + Android), hemskärms-installation och en ärlig integritetsrad. Domänen lämnas som platshållaren `LÄNK`. **Resten av `#73` är manuellt/ops och kräver dig:** peka domänen rätt, verifiera HTTPS + HSTS (checklistan 4.1/4.2 — plattformsnivå), ge tränarna roller, lägg in schemat, och skicka länken. Issuet lämnas öppet tills dess | 
| 2026-09-15 | **E2E: Playwright mot full stack för de fem kritiska flödena** (`#71`, checklistan 12.5, SPEC.md §9) | "Om de fem fungerar, fungerar appen." Riktig backend + Postgres valdes (framför mockad API) för högst trovärdighet | **Fem specar** i `frontend/e2e/`: nästa match utan konto (vaktar att inget anrop bär en token), prenumerera på kalendern (webcal-href + ICS från backend), tränarens massinlägg (login → paste → granska → spara), spelarkortet (lägg till barn, mål → märke, och **noll `/api`-anrop** på spelarkortssidan + inget anrop bär barnets namn, §KM.2), och samåkning i två webbläsarkontexter (erbjudande → förfrågan → nekande med meddelande). **Teststödet är gejtat:** backend registrerar `TestMailbox` + fångande e-postsändare och tre minimala endpoints (`/api/v1/testing/prepare`, `/code`, `/reset-carpool`) **bara** när `Testing:Enabled` — uppstarten faller om flaggan är på i Production, och endpointsen finns inte ens för gästvakten annars. **Inloggningen** drivs genom riktiga UI:t: koden fångas ur mejlet (bara hashad når DB:n) via brevlådan. **HTTPS på Vite-dev** (`E2E_HTTPS=1`, `@vitejs/plugin-basic-ssl`) så refresh-cookien lagras och en sida kan laddas om utan att tappa inloggningen — det som gör fleranvändar-flödet provbart. **CI:** `.github/workflows/e2e.yml` reser Postgres, migrerar+seedar backend, kör Vite-dev över HTTPS, installerar Playwright och kör. **Att bevaka:** kunde inte köras lokalt (ingen webbläsare/Postgres i miljön) — specarna verifieras i första CI-körningen; några selektor-/tidsjusteringar kan behövas. Rad 12.5 är 🟡 tills CI är grönt | 
| 2026-09-15 | **SAST med CodeQL i CI; DAST-workflow (ZAP) redo för staging** (`#70`, checklistan 12.7) | Automatiserad säkerhetsanalys inför lansering. Appen är publik och hanterar personuppgifter — en billig genomgång fångar det uppenbara | **SAST:** `.github/workflows/codeql.yml` kör CodeQL för `csharp` och `javascript-typescript` på push/PR + veckovis, med `build-mode: none` (analyserar källkoden direkt, så .NET 10-bygget inte kan fälla analysen) och `security-and-quality`-sviten. Gratis eftersom repot är publikt. Fynden hamnar under Security → Code scanning, inte som ett fällt bygge — en människa bedömer. **DAST:** `.github/workflows/dast-zap.yml` — OWASP ZAP baseline, **manuell** (`workflow_dispatch`) eftersom DAST kräver en körande miljö; kör den mot staging när en sådan finns (baseline är passiv, skickar inga angrepp). Regelfil `.github/zap-rules.tsv` för att tona ner medvetna icke-fynd. **Beroende-skanningen** (`dotnet list --vulnerable`, `npm audit`) lades redan i CI i `#69` och är ren. **Kvar (extern):** köra DAST mot en deployad staging och bedöma fynden, samt granska CodeQL:s första fynd — bådadera kräver att workflowarna faktiskt kört. Rad 12.7 därför 🟡 | 
| 2026-09-15 | **Säkerhetschecklistan reviderad rad för rad; säkerhetsheaders, CSP, Dependabot och CI-skanning tillagda** (`#69`) | Releasegrinden. Många rader var gröna i koden men aldrig avbockade; några baslinjekontroller saknades helt | **Verifierat och avbockat** ~40 rader mot koden (objektnivå-auktorisering, gäst-läs/inloggad-skriv, gallring, ingen barn-PII, dataminimering, m.m.). **Byggt det som saknades:** `SecurityHeadersMiddleware` (nosniff, `X-Frame-Options: DENY`, `Referrer-Policy`, HSTS över HTTPS, restriktiv CSP för JSON-API:t) + samma headers och en SPA-CSP i `vercel.json`; `.github/dependabot.yml` (nuget/npm/actions); CI-steg som fäller bygget på sårbara paket (`dotnet list --vulnerable`, `npm audit --audit-level=high`) — båda rena idag. **CSP-nyans:** `script-src 'self'` är strikt (noll inline-script i bundeln), men `style-src` kräver `'unsafe-inline'` för dynamiska inline-stilar (lagfärg, märkesstaplar) — rad 5.1 är därför 🟡 tills en nonce-lösning finns och den verifierats i en Vercel-preview. **§KM-rader som inte får bli "ej tillämpliga"** (9.1/9.4/9.5 om barnkoppling) markerades ✅ **genom frånvaro** — ingen trupp/barn-tabell finns på servern (beslut 2026-09-10), så kraven hålls vakuöst vid lansering och återöppnas om trupp införs. **Kvar före lansering, dokumenterat i checklistans fot:** externa åtgärder (7.7 rotera JSONBin-nyckeln, 9.14 biträdesavtal med Resend, 9.12 bekräfta Neon/Render-regioner, 4.6 stäng direktåtkomst till Render), issue-beroenden (12.5 `#71`, 12.7 `#70`, 12.8 `#72`), och mindre poster (8.4 correlation-ID i audit, 8.5/8.6 larm & felrapportering, 10.5 NuGet-lockfiler, 11.5 maintenance-sida). Testat: 5 nya integrationstester för säkerhetsheadrarna | 
| 2026-09-15 | **Gallring i drift: tysta push-prenumerationer städas, gamla säsonger behålls** (`#68`, checklistan 9.8) | Dataminimering i praktiken. Tre av fyra delar fanns redan; den som saknades var proaktiv gallring av prenumerationer som aldrig hör av sig | **Push-prenumerationer, två vägar:** den **döda** (push-tjänsten svarar 404/410) städas redan **reaktivt** vid utskick (`PushDispatchWorker` → `RemoveAsync`). Den **tysta** — en telefon som lagts undan och därför aldrig får ett utskick att svara på — sveps nu upp av en ny `PushRetentionService` + `IPushRetentionRepository`, gräns **12 månader**. Tystnad mäts på `LastUsedUtc`, och för den som aldrig fått ett utskick på `CreatedUtc` (fältet är null tills första lyckade leverans) — annars hade en aldrig-använd prenumeration aldrig gallrats. Raderar via id-stubbar, så endpoint/nycklar (secrets, §KM.10) aldrig läses till minnet. Kör **i processen** via `PushRetentionWorker` (spegel av `CarpoolRetentionWorker`, 24h), **inte** en andra Vercel-cron — dygnsslotten är tagen av påminnelsen (§KM.11), och Render-kallstarter kör den ändå flera gånger om dagen. **Samåkning 30 dagar:** redan klart sedan `#55` (`CarpoolRetentionWorker`). **Gamla säsonger gallras medvetet INTE:** gammal matchdata är inte personuppgift (§KM.1), den innehåller inga barn, och kalenderfeeden (§KM.4) refererar matcherna — att radera dem skulle tömma föräldrarnas prenumerationer utan integritetsvinst. Står i issuets *Vad* men inte i dess *Klart när*; ett eget beslut krävs om det någon gång ska införas. Testat: tyst tas bort, färsk behålls, aldrig-använd bedöms på skapelsetid, idempotent | 
| 2026-09-15 | **Registerutdrag: `GET /api/v1/auth/export`, läsbart och secret-fritt** (`#67`, checklistan 9.13) | GDPR:s rätt till registerutdrag. Blir här nästan tomt, vilket är själva poängen — servern lagrar så lite att utdraget ryms på en skärm | Ett eget läsförråd `IAccountExportRepository` i stället för en "lista per konto" på vart och ett av fem repon: joinsen mot match och lag samlas på ett ställe (utdraget säger "match mot X", inte ett GUID — kravet är att det är läsbart för en människa), och push-repot behöver inte luckra upp sitt medvetna förbud mot att lämna ut endpoint/nycklar. **Push-projektionen väljer aldrig ut secreten** (§KM.10) — den kan inte råka följa med. Utdraget bär konto, samåkningserbjudanden, förfrågningar, närvarosvar, notisinställningar och prenumerations­sammanfattning; enum blir läsbara namn, tider i UTC (§KM.5, FE visar svensk tid). **Spelarkortet nämns som frånvarande** med hänvisning till säkerhetskopieringskoden — det når aldrig servern (§KM.2), så det kan inte lämnas ut, och det sägs rakt ut. Typen döptes `DeviceOnlyDataNoticeDto` (inte `PlayerCard…`) för att inte fällas av §KM.2-arkitekturvakten — den vakten luckras inte upp för en notis om en frånvaro. FE: sektion "Mina uppgifter" på Mitt konto som hämtar på begäran, visar läsbart på svenska och laddar ner som JSON-fil. Endpointen är `private` (ingen edge-cache), objektnivå-auktoriserad (id ur token). Testat: 401 för gäst, bara egen data, secreten aldrig i svaret, spelarkortsnotisen med | 
| 2026-09-15 | **Integritetstexten publicerad — skriven för föräldrar, och den säger EU-överföringen rakt ut** (`#66`) | §KM.6 kräver en begriplig integritetstext (öppen fråga 7). Den ska inte gömma det obekväma bakom "vi kan komma att dela med underleverantörer": mejladressen lämnar EU när inloggningskoden skickas, via Resend som lagrar sändningsloggar i USA, på grunden **Data Privacy Framework** — leverantör och grund namngivna, precis som checklistans rad 9.14 kräver | FE-sida på `/integritet` (`features/privacy`), **publik** så att en gäst når den innan hen loggar in — länkad från foten på varje sida och från Mitt konto. Täcker: spelarkortet stannar i telefonen och kan gå förlorat utan säkerhetskopia (§KM.2), kontot lagrar bara en vuxens mejladress/namn (§KM.1), samåkningens fritext gallras 30 dagar efter matchen (§KM.12), inga telefonnummer efterfrågas, ingen spårning (bara Open-Meteo + kartlänkar), och EU-överföringen ovan. Ren text, ingen ny endpoint, ingen ny kolumn, ingen PII lämnar enheten. Testat: sidan nås utan inloggning, och de känsliga påståendena (device-only, 30-dagarsgallring, Resend/USA/DPF, ingen spårning) står faktiskt i texten. **Människogrind kvar:** att någon som inte byggt appen läser och godkänner texten — sker i PR-granskningen |
| 2026-09-14 | **Samåkningsnotiser: rätt mottagare per händelse, aldrig fritext** (`#63`) | §KM.12: nytt erbjudande → laget; ny förfrågan → föraren; svar → den som frågade; tillbakadraget → de accepterade. Utan notiser tappar samåkningen mot gruppchatten | Köas i de tre carpool-tjänsterna efter save (§KM.11). Texten säger bara <em>att</em> något hänt — förarens notis, den frågandes hälsning och nekandets meddelande är alla fritext (§KM.12, §KM.10) och når aldrig en låsskärm; "öppna för att se" tar in i appen. Alla länkar till `/match/{id}`. Testat med inspelande outbox: rätt mottagarkrets (lag vs konton) per händelse, och att fritexten aldrig läcker in. **Låser dessutom upp `#58`:s uppskjutna del** — konto↔prenumeration-kopplingen som "inte svarat + påminnelse" väntade på finns nu |
| 2026-09-14 | **Push vid matchändring köas i handlern, texten säger vad som ändrats** (`#62`) | En notis som bara säger "matchen har ändrats" tvingar alla att öppna appen. `MatchAdminService` köar därför en notis vid **skapa, flyttad tid, ny plats och inställd** — men skiljer på dem i texten: "Ny tid: lördag 14:00", "Ny plats: Skarpe Nord", "Inställt". Radering köar ingen notis (inte en av de fyra, och en notis mot en raderad match blir en 404); en ändring av bara notistexten köar heller ingen — en förälder ska inte väckas för det | Köas i utkorgen (`#61`) efter att matchen sparats, aldrig i requesten (§KM.11). Texten bär bara sådant som ändå står i det publika schemat — motståndare, tid, plats — **aldrig tränarens notis** (fritext, §KM.1), aldrig barn (§KM.1) eller spelarkort (§KM.2). Tiden formateras via `SwedishTime` (§KM.5), inte en egen uträkning. Testat med en inspelande `IPushOutbox`: rätt notis vid rätt händelse, ingen vid radering/notistext-ändring, och att fritexten aldrig läcker in |
| 2026-09-01 | **Audit-loggen saknar främmande nyckel till kontot** | Posten om en radering måste finnas kvar efter att kontot är borta. En nyckel hade antingen raderat posten med kontot eller hindrat raderingen | Posten är avsiktligt mager: vem som id, vad, och när — aldrig en adress (§KM.10). En audit-logg som samlar personuppgifter är själv ett integritetsproblem, och skulle dessutom överleva just den radering den beskriver |
| 2026-08-31 | **Förnyelsen är enkelspårig i klienten** | Servern roterar refresh-token och tolkar en återanvänd token som en stöld — då återkallas hela familjen. **Två samtidiga förnyelser från vår egen klient ser likadana ut**, så utan spärren hade appen loggat ut användaren själv, och felet hade sett ut som ett serverfel | Alla som frågar delar på samma pågående anrop. Fallet är inte teoretiskt: en skyddad route och `AuthProvider` startar samtidigt vid en direktladdning, och två anrop kan få 401 i samma ögonblick |
| 2026-08-31 | **En flagga i lagringen avgör om appen ens försöker förnya** | Utan den hade varje besök gjort ett anrop mot inloggningen — även för de allra flesta som aldrig loggar in. Det anropet går inte att cacha på Vercels edge och hade väckt Render varje gång någon öppnade schemat, alltså precis de femtio sekunder §KM.11 finns till för att slippa | Flaggan är **ingen behörighet**: den som sätter den för hand blir inte inloggad, bara mött av ett 401. Access-token ligger fortsatt enbart i minnet |
| 2026-08-31 | **Roller ligger i token, med lagen som slug** | Auktorisering ska inte kosta en databasfråga per anrop. Appens adresser är byggda på slug (`/lag/gul`), så en kontroll mot laget i en route blir en jämförelse av två strängar | Ett anspråk per lag en tränare ansvarar för, plus `role=admin`. **Priset: en ändrad roll slår igenom först när token förnyas, som mest en kvart.** Omedelbart återkallande kräver att sessionen avslutas. En omdöpt slug gör en tränares behörighet ogiltig tills token förnyas — slugen ligger i länkar föräldrar delar och är avsedd att vara stabil |
| 2026-08-31 | **Tränarrollen finns inte utan ett lag** | En roll som betyder "tränare i allmänhet" är en roll som gäller alla lag den dag någon glömmer objektnivåkontrollen | `TeamRole` kräver `TeamId` för Coach och förbjuder det för Admin, bevakat av ett villkor i databasen. Kravet `CoachOfTeam` **nekar när laget saknas i adressen** — ett krav som inte hittar sitt lag och därför släpper igenom vore värre än inget krav alls, eftersom det ser ut att skydda |
| 2026-08-31 | **Sexsiffrig engångskod, med tre spärrar som gör den försvarbar** | Koden ska gå att läsa i ett mejl och skrivas av på en telefon med en hand. Sex siffror är en miljon möjligheter, vilket i sig inte är något skydd | Livstid 10 minuter, engångsanvändning, och **spärr efter fem felgissningar per kod**. Var och en för sig är otillräcklig; det är kombinationen som håller. Koden slumpas med `RandomNumberGenerator.GetInt32` — inte `Random`, som är förutsägbar — och lagras hashad, så en läckt databas inte innehåller giltiga inloggningar |
| 2026-08-31 | **Svaret är detsamma för känd och okänd adress** | En inloggningsruta som svarar olika är en adresslista för den som frågar tillräckligt många gånger | `request-code` svarar alltid 202, `verify-code` alltid samma 401 oavsett om koden var fel, utgången, förbrukad, slutgissad eller adressen okänd. **Kontot skapas först vid lyckad verifiering** — en adress som aldrig loggat in lämnar inget efter sig, och tabellen går inte att fylla med påhittade adresser |
| 2026-08-31 | **Utan API-nyckel faller uppstarten i drift, men inte lokalt** | Att tyst låta bli att skicka inloggningskoder vore det sämsta utfallet: allt ser ut att fungera, ingen kommer in, och felet upptäcks när en förälder hör av sig | I drift kastas ett fel vid start. I utveckling skrivs mejlet i konsolen i stället, eftersom koden lagras hashad och annars inte går att få tag på. Att utvecklingsavsändaren aldrig kan väljas utanför utveckling är testat — den loggar både adress och kod, vilket §KM.10 annars förbjuder |
| 2026-08-30 | **Kontot lagrar en mejladress — projektets första personuppgift på servern** | Inloggningen kräver något att knyta sessionen till. Adressen är en **vuxens**, inte ett barns, så §KM.1:s tak är orört — men regeln kräver att beslutet skrivs, och det görs här | `Accounts` lagrar id, adress, skapad och senast inloggad. Inget namn, inget telefonnummer, ingen koppling till ett barn. Adressen normaliseras till gemener med unikt index, loggas aldrig (§KM.10), och `RefreshTokens` kaskaderar bort med kontot. Spelarkortet berörs inte: det når fortfarande aldrig servern (§KM.2) |
| 2026-08-30 | **Refresh-tokens lagras hashade, roteras och faller familjevis** | Sessionerna är långlivade (60 dagar), så en stulen token är värdefull länge. Rotation gör den kortlivad i praktiken; hashning gör en läckt databas oanvändbar; familjen gör återkallandet meningsfullt | SHA-256 i stället för klartext. Varje förnyelse ersätter, och en redan förbrukad token fäller **hela kedjan** — inte bara sig själv, för annars behåller tjuven nästa token. Access-token lever 15 minuter, refresh 60 dagar: appen används säsongsvis och en förälder som öppnar den en gång i månaden ska inte mötas av inloggning |
| 2026-08-30 | **Eget CSRF-filter i stället för ramverkets `[ValidateAntiForgeryToken]`** | Attributet ligger i MVC:s vy-maskineri och registreras bara av `AddControllersWithViews()`. I ett JSON-API med `AddControllers()` **kastar** det i stället för att skydda, vilket syns som ett 500 utan förklaring | `RequireCsrfTokenAttribute` gör samma kontroll mot `IAntiforgery` och svarar med ProblemDetails. Ett test kontrollerar att varje tillståndsändrande endpoint under `/auth` bär det, så en ny endpoint inte kan glömma bort skyddet |
| 2026-08-30 | **Anti-forgery-cookien kräver HTTPS bara utanför utveckling** | `CookieSecurePolicy.Always` gör att antiforgery kastar när anropet går över HTTP — det hoppar alltså inte bara över attributet. Lokalt och i tester går anropen över HTTP | I drift `Always` (Render avslutar TLS och `UseForwardedHeaders` körs först, så anropet är HTTPS när antiforgery tittar); i utveckling `SameAsRequest`. Skillnaden är enhetstestad, eftersom felet annars bara syns som ett oförklarat 500 |
| 2026-08-30 | **Kommandosidan i dispatchern infördes här** | Förnyelse och utloggning ändrar tillstånd, och CLAUDE.md kräver att commands och queries skiljs åt. `#30` är det första användningsfallet som skriver | `ICommand`, `ICommandHandler`, `ICommandDispatcher` och en egen valideringsbehavior speglar frågesidan. Att slå ihop dem hade sparat ett femtiotal rader men krävt att den fungerande frågevägen skrevs om — det hör hemma i en egen ändring, inte i den som inför inloggningen |
| 2026-08-30 | **Lagfärgen får bära text — med bläck som räknas fram ur färgen** | Stilmallen sa tidigare att lagfärgen aldrig fick ligga bakom text, eftersom Vit och Svart inte går att hålla kontrast mot. Det var att lösa fel problem: det som inte fungerar är en *fast* textfärg ovanpå fyra olika lagfärger. Räknas bläcket fram per färg klarar alla fyra god marginal, och det gäller även den femte färg en tränare skriver in i M3 | `lib/teamTheme.ts` väljer svart eller vitt efter vilket som ger mest kontrast. **Bläcket är rent svart och inte `--text-strong`:** med nästan-svart faller sämsta fallet till 4,45:1 och under AA — hittat av ett test som sveper färgrymden, inte av ögat. Lagfärgade ytor behöver dessutom alltid en ram, annars försvinner Vit i ljust läge och Svart i mörkt |
| 2026-08-30 | **Utseendet blir en egen milstolpe (M1.5)** | Boarden hade noll designissues — alla 45 backlog-poster var funktion och säkerhet. Att appen ser generisk ut har en konkret orsak: typsnitten har aldrig legat på boarden, och `#26`, som kommentaren i `index.css` hänvisar till, hade dem aldrig bland sina kriterier | Fyra issues (`#115`–`#118`), enbart frontend, körs parallellt med M2. Lagfärgen får aldrig bära text — kontrasttesterna visar redan Gul på 2,15:1 och Vit på 1,32:1 |
| 2026-08-29 | **Rate limiting och DB-backup lyfts till baslinje** | Publika endpoints på öppet internet; familjestatistik går inte att återskapa | Avvikelse §KM.0 A1 och A2 — byggs i M0–M1 i stället för "vid behov" |

## ❓ Öppna frågor

| # | Fråga | Blockerar | Ägare |
|---|-------|-----------|-------|
| 1 | ~~Har klubben redan ett officiellt verktyg?~~ **Besvarad 2026-08-29:** ja, men det används inte. | — | ✅ |
| 2 | ~~Vill tränarna använda appen?~~ **Besvarad 2026-08-29:** ja. | — | ✅ |
| 2b | **Var kommer matchschemat ifrån i dag?** Avgör om automatisk import är värd att bygga senare. Icke-blockerande. | — | Haval |
| 2c | ~~**Vilken mediator ska Application använda?**~~ **Besvarad 2026-08-30:** handskriven dispatcher, se ADR ovan. Tidigare formulering: MediatR 13+ ligger under RPL-1.5 eller kommersiell licens. RPL kräver källkodspublicering vid **driftsättning**, inte bara distribution — vilket krockar med ett privat repo som servar en publik app. Alternativ: handskriven dispatcher (~40 rader, rekommenderat), MediatR 12.4.1 fastlåst (Apache-2.0 men fryst), eller acceptera RPL-copyleft på vår egen kod. **Behövdes innan första handlern**, vilket visade sig vara `#16` i M1 — inte M3. | — | ✅ |
| 3 | ~~Var driftas backend?~~ **Besvarad 2026-08-29:** Vercel + Render + Neon, se ADR ovan. | — | ✅ |
| 4 | ~~**Vilken e-postleverantör** för inloggningskoden?~~ **Besvarad 2026-08-30:** Resend, med dokumenterat undantag från checklistans 9.12 — se ADR ovan. Kravet på EU-hosting är därmed medvetet uppmjukat, inte bortglömt. | — | ✅ |
| 5 | **Domännamn** — `karramatcher.se` föreslaget, tillgänglighet ej kontrollerad. **Nedgraderad 2026-08-30: blockerar M8, inte M2.** Beslut: vi kör utan domän tills vidare. Effekten är dock inte den man tror — utan verifierad domän svarar Resend **403** och skickar bara till kontots egen adress, alltså inget mejl alls till föräldrarna, inte ens i skräpkorgen. Det räcker ändå för att bygga och testa hela M2 mot Havals egen adress. Domänen behövs först när hundra föräldrar ska få koder. Billigaste vägen dit: en underdomän till `carcheck.se`, som redan ägs och är i drift. | **M8** | Haval |
| 10 | ~~**Ska barnuppgifter någonsin få finnas på servern?**~~ **Besvarad 2026-09-10: nej.** Inga uppgifter om barn lagras på servern över huvud taget — ingen `Player`, ingen trupp, ingen kolumn. §KM.1 är skärpt, §KM.2, §KM.6 och §KM.7 följdändrade, och M6 omskriven: kallelsen räknar svar från vuxna konton i stället för att namnge barn. `#40` och `#59` är därmed stängda som inaktuella. | — | ✅ |
| 6 | **Vem är admin och vilka är tränare?** Riktiga personer krävs före lansering. | M7 | Haval |
| 7 | **Samtyckestexten** — behövs först när en tränare lägger upp truppen, eftersom servern annars inte behandlar några barnuppgifter alls. Integritetstexten är nu skriven och publicerad på `/integritet` (`#66`); kvar är att någon utanför bygget läser och godkänner den, vilket sker i PR-granskningen. | M6 | Haval + klubben |
| 9 | **Hur hittar föräldrar varandra vid samåkning?** Vi lagrar inga telefonnummer. Räcker namn plus meddelandefältet? | M5 | Haval — pröva med tränarna |
| 8 | **Kärra KIF:s namn och märke** — får appen använda dem, eller ska den vara tydligt inofficiell? | M7 | Haval |

## ⚠️ Kända risker & blockerare

| Risk | Konsekvens | Plan |
|------|-----------|------|
| **Tränarna använder den inte** | Appen är tom och allt annat spelar ingen roll. Detta är den enskilt största risken. | Förankra före M2. Massinlägget måste vara så snabbt att det slår gruppchatten första gången de provar |
| **Säsongsbunden användning** | Appen glöms bort mellan säsongerna | Kalenderfeed och push levererar värde utan att appen öppnas — därför ligger de i v1 |
| **Renders kallstart** | Backend somnar efter ca 15 min tystnad och tar ~50 s att vakna. Appen används mest lördag morgon, efter en tyst natt — första föräldern får vänta | Publika GET-svar cachas på Vercels edge så vanliga sidvisningar aldrig väcker backend; ett gratis uppetidsverktyg pingar `/health`; UI:t säger ifrån på svenska vid långsamt svar (§KM.11) |
| **En misslyckad driftsättning märks inte** | Render låter den gamla containern ligga kvar när den nya inte startar. API:t svarar, CI är grön och `/health` ger 200 — men koden är gammal. Så gick det 30 augusti till 6 september: 34 sammanslagningar till `main`, varav 12 rörde `backend/src`, nådde aldrig drift — och det syntes först när någon slog i en endpoint som saknades | `render.yaml` listar nu varje obligatorisk hemlighet med `sync: false`, så en saknad sådan syns som en tom ruta i dashboarden. Kvarstår: **ingenting larmar när en driftsättning fallit** — kontrollera *Events* i Render efter varje merge till `main` tills något gör det automatiskt |
| **iOS-push kräver hemskärm-installation** | En del föräldrar får aldrig notiser | Kalenderfeeden är den primära kanalen; push är komplement. Installationstips visas på iOS |
| **Barn-PII** | Ett läckage av barnuppgifter vore allvarligt, oavsett hur litet projektet är | Kraftigt reducerad: spelarkortet når aldrig servern (§KM.2), och truppen finns bara om kallelsen aktiveras. §KM.1 sätter taket på det som ändå lagras |
| **Spelarkortet kan gå förlorat** | Telefonbyte, rensad webbläsardata eller iOS som gallrar lagring för en app som inte använts på en vecka → en hel säsong borta | Säkerhetskopieringskod som förstaklassfunktion, `navigator.storage.persist()`, tydlig uppmaning att installera på hemskärmen, och ärlig text om var datan finns (§KM.2) |
| **Gamla JSONBin-nyckeln lever kvar** | Master-nyckel i en telefons localStorage ger åtkomst till hela JSONBin-kontot | Rotera nyckeln på jsonbin.io. Checklistan rad 7.7 |
| **Ingen branch protection på GitHub** | `main` är oskyddad på GitHub-sidan. Ett misstag kan pusha direkt förbi PR-flödet | **Accepterad risk tills vidare** (beslut 2026-08-29). `.githooks/pre-push` blockerar push till `main` lokalt — aktiveras med `git config core.hooksPath .githooks`, en gång per klon. Repot är numera publikt, så riktig branch protection är gratis och kan slås på när som helst |
| **Gamla objekt kvar på GitHub efter historikomskrivning** | Att skriva om historik raderar inte gamla objekt hos GitHub — de nås via sina SHA:n tills GitHub kör städning, och SHA:na syns i commitlistorna på mergade PR:ar. Verifierat: den borttagna filen gick att hämta via `?ref=<gammal SHA>` även efter omskrivningen | **Accepterad risk** (beslut 2026-08-29) — innehållet granskades och bedömdes okritiskt. **Lärdom:** kontrollera vad en mapp innehåller *innan* den checkas in; en omskrivning i efterhand är aldrig fullständig utan att be GitHub köra `gc` |
| **Licensfläck från ett beroende** | Ett copyleft-licensierat bibliotek kan tvinga fram publicering av vår källkod — RPL-1.5 redan vid driftsättning | Kontrollera licensen innan ett paket läggs in, inte efteråt. Upptäcktes på MediatR i #2, se öppen fråga 2c |
| ~~**Spelplatsernas koordinater är avrundade**~~ **Rättad 2026-08-30 (#110)** | Felet var värre än noterat: upp till **2,2 km**, inte 1,1. Det påverkade väderprognosen, som vid kusten kan skilja mellan regn och uppehåll | Sex av sju rättade mot verifierade positioner, via en datamigration. **`Kode IP` står kvar orörd** — OpenStreetMap har fyra namnlösa planer i Kode och ingen namngiven idrottsplats, och ett gissat värde hade sett lika rätt ut som ett verifierat. Ett test fäller bygget om någon avrundar igen. Navigationen använder fortsatt adressen och påverkas inte |
| **Framtida spelplatser får koordinater för hand** | Varje spelplats en tränare lägger upp i M3 kommer att ha samma problem som de sju hade, och ingen tränare skriver latitud och longitud rätt | Koordinaterna ska härledas ur adressen vid sparande, aldrig skrivas in. Geokodningen blir då samtidigt en adresskontroll: går adressen inte att hitta får tränaren veta det direkt i stället för att en förälder upptäcker det en lördagsmorgon. Infört som krav i `#37` |
| **Renders timbudget kan släcka appen** | 750 fria instanstimmar per månad och arbetsyta. När de tar slut suspenderas alla fria tjänster till den 1:a nästa månad — appen nere i upp till 30 dagar | Fönstrad ping håller förbrukningen kring 251 h/månad. **Ingen andra fri Render-tjänst får läggas till utan att räkna om budgeten.** Kontrollera förbrukningen i Renders dashboard inför säsongsstart |
| **Data Privacy Framework kan falla** | Inloggningsmejlen går via Resend, som lagrar loggar med föräldrarnas mejladresser i USA. Överföringen vilar på DPF. EU:s tribunal fastställde det i september 2025, men ett överklagande ligger hos EU-domstolen och Dataskyddsstyrelsen bad kommissionen ompröva det den 31 juli 2026. Safe Harbor och Privacy Shield föll båda på samma sätt | **Accepterad risk** (beslut 2026-08-30). Faller DPF behöver överföringen vila på SCC i stället, eller leverantören bytas. Bytet är billigt om det förbereds: håll utskicket bakom ett eget gränssnitt i Application-lagret så att implementationen kan bytas utan att röra inloggningsflödet. Scaleway TEM är det förberedda alternativet |
| **Neons retentionsfönster är 6 timmar** | Bekräftat 2026-08-29 — det är taket på fri nivå. En trasig migration som driftsätts fredag kväll och upptäcks lördag morgon ligger redan utanför fönstret, och det är precis då appen används | Den logiska dumpen är därmed det egentliga skyddsnätet, inte en reserv. Tas före **varje** migration mot produktion och efter att säsongens schema lagts in. Rutinen står i `docs/DATABAS-BACKUP.md`. Längre fönster kräver betald Neon-plan — omprövas om datamängden växer |
| ~~**Vercel kanske inte edge-cachar en extern rewrite**~~ **Avskriven 2026-08-30** | Antagandet höll. Två anrop i rad gav `MISS` följt av `HIT`, med **identiskt `Rndr-Id`** — Render utfärdar ett nytt id per request, så backend kontaktades aldrig på det andra anropet | Verifieringen står i `README.md`. Kvarstår att veta: Vercel försvagar vår ETag till `W/"…"`, vilket vår `If-None-Match`-tolkning hanterar. Tas den hanteringen bort slutar `304` fungera i drift utan att CI märker det |
| **Ensam utvecklare** | Ingen annan kan ta över, och PR-granskning görs av samma person som skrev koden | Handoff-filen och ADR-tabellen hålls aktuella så en ny person kan kliva in. `/code-review` och `security_reviewer` används som andra ögon |
